# 独立论文管理与阅读工作台

日期：2026-08-21
状态：渐进式设计收敛中（切片 A、B、C 已完成 macOS 实施与验收；切片 D 方向与 D0 技术验证已完成，实施计划待确认；各平台结果按测试模块记录）
分支：`feat/research-workbench`
关联需求：`TASK-013`（仅作长期追踪编号；本文档不改变外部任务状态）

> 本文档是 Research Workbench 的持续方向设计记录与当前设计真源。由于功能复杂，文档会随讨论和
> 技术验证逐步补充已确认方向、设计决策、待验证假设、状态机、失败恢复语义和验收标准。它不是一份
> 覆盖所有切片的实现计划；每个切片达到准入条件后，再单独形成一份逐文件实施计划。这里所说的
> “方向设计”仍会记录已经确定的具体语义，并不只保留宏观概念。本文档不授权访问或更新任何外部
> 任务系统。关联需求中的视觉原型只是信息结构示例，不是必须复刻的界面、流程或验收标准。

---

## 1. 产品定位

Research Workbench 是 Personal WorkBench 内一个独立的论文管理、阅读与研究知识整理领域。
它不是 Zotero 的附属品，也不以 Zotero 数据库作为内部真相来源。

产品应当拥有自己的数据模型、文件管理、PDF 阅读、批注、检索和研究工作流。Zotero、
BibTeX、RIS、CSL JSON 等只位于互操作边界，用于迁移、导入、导出和必要的生态兼容。

目标工作流是：

```text
采集 / 导入
    → 去重与元数据确认
    → 文献库、目录与标签
    → PDF 阅读与上下文批注
    → 证据、观点与跨论文对照
    → 综述、写作和研究项目推进
```

产品差异化不来自“比 Zotero 多几个按钮”，而来自两点：

1. 更清晰、可恢复、不会重复占用空间的文件与知识组织方式。
2. 把“读过和高亮过”继续推进为可追溯的证据、观点和写作材料。

## 2. 当前已确认的痛点

### 2.1 文件保存与分类

- 同一 PDF 可能服务多个论文、课程或研究项目，不应因为出现在多个层级目录而重复保存。
- 同一 PDF 在不同研究目的下需要不同批注，不能被迫共享唯一一套标注。
- 文件夹树必须清晰直观，移动、添加到其他目录和删除引用的语义不能含糊。
- 文件去重不能破坏用户对论文版本、所属项目和阅读上下文的理解。

### 2.2 标签创建、治理与找回

- 标签入口过小或创建步骤繁琐会打断阅读。
- 标签长期积累后容易出现大小写、语言、单复数和近义词造成的重复。
- 标签如果只以大量小色块平铺，数量增加后无法浏览和维护。
- 用户有时记得概念或近义表达，却想不起当时使用的准确标签。

### 2.3 PDF 资源开销

- 打开过多 PDF 后，后台标签页不应持续占用完整渲染内存。
- 大型文档的解析、页面渲染、全文索引和 OCR 不应在打开瞬间同时争抢 CPU。
- 关闭或休眠文档后，页面 Canvas、文本层和中间缓存必须能够回收。

### 2.4 论文类型与引用元数据

- 论文、报告、预印本、标准、网页和其他材料不一定能落入严格的固定分类。
- 不完整或不规则来源不能因为缺少“正确类型”而无法保存。
- 作者、日期、载体、版本和标识符等引用信息需要灵活保存，并保留来源与原始字段。
- 引用输出格式不应反过来限制内部数据模型。

### 2.5 基础能力底线

- 基本 PDF 阅读器能力不能缺失。
- Zotero 已被验证有效的文献管理能力不能因为产品追求差异化而丢失。
- 兼容 Zotero 是次要目标，但用户的数据必须可迁移、可导出，不能形成新的锁定。

## 3. 已确认的产品原则

### 3.1 WorkBench 是数据真源

Research Workbench 自己管理论文、元数据、附件索引、阅读状态、批注和研究知识。
外部平台 ID 只用于建立映射，外部数据不得直接决定内部表结构。

### 3.2 论文、文件和上下文分离

“学术作品”“某一版本”“磁盘上的文件”“目录中的位置”和“为了某个目的阅读”是不同概念。
不能用一条记录或一个目录路径同时承担这些职责。

### 3.3 原始文件默认不可变

批注、笔记和阅读状态默认旁挂保存，不直接修改原 PDF。需要分享时可显式导出带批注副本。
这样才能可靠去重、校验文件完整性，并避免第三方工具写回导致原件变化。

### 3.4 本地优先、可恢复、可迁移

- 离线时仍能管理、阅读、搜索已有内容。
- 原始 PDF、元数据、批注和笔记都有明确导出路径。
- 导入、合并、移动和删除等高风险动作应可预览或撤销。
- 数据库损坏不能连带破坏原始附件。

### 3.5 性能是功能约束

大型 PDF、多文档标签页和长时间阅读是正常场景，不是事后优化项。实现前必须建立测试语料和
测量首次打开、翻页、搜索、峰值内存、后台休眠及关闭后回收等指标。

### 3.6 视觉原型不构成约束

与 `TASK-013` 关联的视觉材料仅用于说明“论文项目、阶段、研究资产、导师意见”等可能的
信息结构。最终导航、布局、步骤数量、进度表达和交互方式均需根据真实阅读工作流重新设计。

### 3.7 跨平台与运行形态

首版继续采用浏览器界面 + 本地 server，不引入桌面外壳。Windows 和 macOS 是正式支持平台，
Linux 作为兼容目标，在文件路径、进程启动、PDF 能力和凭据存储等平台差异上避免主动封死。
未来若转为 App，应作为完整运行形态迁移单独设计，不在当前方案中预埋半套桌面实现。

跨平台验证以具体测试模块为单位。只有涉及文件系统、路径、进程、凭据存储或原生依赖的平台相关模块，才维护
对应的平台测试矩阵；不要求每个切片在不同平台重复整条产品流程。一个模块可以先在当前开发平台完成实现和测试，
另一平台的同模块结果保留为待测，不阻塞无关模块。若同一模块先在 Windows 验证，之后补 macOS；先在 macOS
验证时则补 Windows。每条兼容性记录必须写明测试模块、操作系统、架构、文件系统、Node.js 版本和测试日期；
没有运行过的平台明确标为“未实测”，不能从路径语法检查或其他平台结果推定为通过。

## 4. 领域概念草案

下表用于稳定讨论语言，不等同于最终数据库表。

| 概念                | 职责                                                                         |
| ------------------- | ---------------------------------------------------------------------------- |
| `Work`              | 一项学术作品或研究材料的稳定身份，例如论文、报告、标准或网页                 |
| `Edition`           | 作品的具体版本，例如 arXiv 预印本、会议版、期刊版和修订版                    |
| `Asset`             | 按内容 hash 识别的不可变字节内容；文件身份不由路径决定                       |
| `AssetLocation`     | `Asset` 的实际存储位置与状态，可为按账号管理的托管位置或用户磁盘上的链接位置 |
| `Attachment`        | `Edition` 与 `Asset` 的关系及文件角色，例如正文、补充材料或数据附件          |
| `Collection`        | 用户可见的层级目录或由保存查询形成的智能集合                                 |
| `CollectionEntry`   | `Work` 在某个普通目录中的逻辑引用，不复制物理文件                            |
| `Tag`               | 绑定 `Work`、具有稳定 ID 和规范名称的跨目录描述                              |
| `TagAlias`          | 标签的语言变体、缩写、旧名称和近义入口                                       |
| `WorkRelation`      | 两个 `Work` 之间的手工关联，不预先引入证据或观点语义                         |
| `ImportSession`     | 导入箱中一次可取消、可恢复和可对账的导入草稿及处理状态                       |
| `MergeRecord`       | 作品合并历史、原 ID 重定向和可恢复信息                                       |
| `ExternalSourceMap` | Zotero、DOI、arXiv 等外部实体与内部对象的映射及同步游标                      |
| `ResearchProject`   | 毕业论文、课程报告、综述或其他长期研究目标                                   |
| `ReadingContext`    | 一次具有明确目的的阅读上下文，通常关联研究项目或写作主题                     |
| `AnnotationLayer`   | 某个阅读上下文下可独立显示、隐藏和导出的批注集合                             |
| `Annotation`        | 高亮、下划线、区域标注、评论、书签等定位到具体文件版本的标注                 |
| `ResearchNote`      | 属于一个阅读上下文、可关联研究资源的自由笔记                                 |
| `Evidence`          | 从原文和批注提炼、保留准确出处的证据卡片                                     |
| `Claim`             | 可由多篇论文的证据支持、反驳或限定的研究观点                                 |
| `ClaimEvidence`     | 观点与证据之间带有支持、反驳或限定语义的关系                                 |
| `ComparisonMatrix`  | 以作品为列、以观点或比较维度为行的跨论文对照                                 |
| `WritingDocument`   | 由正文和研究资源引用块组成的轻量写作材料                                     |

### 4.1 身份与关系

- `Work` 可以在没有 PDF 或其他附件时独立存在，不完整元数据也不阻止保存。
- 一个 `Work` 可以拥有多个 `Edition`，并记录一个可由用户修改的首选版本。系统可以建议最新或
  元数据最完整的版本，不自动替换用户选择。
- 一个 `Edition` 可以通过多个 `Attachment` 关联正文、补充材料、数据或其他文件。切片 A 负责
  保存、打开和导出通用附件，只对 PDF 提供内容预览边界。
- `Asset` 表达内容身份，可以被多个 `Attachment` 引用，也可以同时拥有一个托管位置和多个链接位置。
- `Work` 可以属于多个普通目录、关联多个标签，并与其他 `Work` 建立手工相关关系。
- DOI、arXiv ID 等标识符按方案和值规范化后参与重复检测，不设为数据库硬唯一约束。重复标识符
  触发高置信度警告，仍允许保存外部脏数据或人工确认的例外。

### 4.2 两级去重与合并

必须区分：

1. **文件级精确去重**：相同字节得到相同 hash，复用一个 `Asset`，具体磁盘位置由
   `AssetLocation` 表达。
2. **作品级候选重复**：PDF 字节不同，但 DOI 相同或标题、作者、年份高度近似；只提示处理，
   不得自动破坏版本关系。

文件级精确去重采用 SHA-256。`Asset` 同时保存算法名和小写十六进制摘要，托管内容使用
`sha256/<前两位>/<后两位>/<完整摘要>` 寻址；文件名、标题和作者不进入对象路径。

候选重复处理时，用户可以选择关联到现有版本、作为现有作品的新版本、仍新建作品、暂留导入箱或
放弃导入。作品合并保留 `MergeRecord`、原 ID 重定向和外部来源映射，在永久清理前可以撤销。

线性化方式、下载站点或嵌入元数据不同都可能改变 PDF 字节，因此只用 hash 无法识别同一作品。

## 5. 文件与目录模型

### 5.1 一份文件，多处出现

- `Collection` 形成单父节点的层级树；同一父目录下名称不重复，不同父目录下可以同名。
- `Work` 与 `Collection` 是多对多关系。
- 把论文添加到另一个目录只创建 `CollectionEntry`。
- 从一个目录移除论文只删除该引用，不删除 `Work`、`Asset` 或其他目录引用。
- 删除目录只删除目录和其中的逻辑引用，不把作品移入回收站。

### 5.2 文件夹视图

至少需要：

- 左侧目录树、展开状态、当前路径和数量。
- “导入箱、全部文献、未分类、最近阅读、重复项、缺失文件、回收站”等系统视图。
- 面包屑和清晰的多目录归属提示。
- 拖拽时由用户明确选择“移动当前引用”或“添加到另一个目录”，不根据拖拽位置猜测。
- 批量归档、移动、添加标签和检查重复项。
- 结构化保存查询形成用户自建智能目录；系统视图仍由系统维护。
- 文件夹与标签职责分离：文件夹表达主动组织，标签表达跨目录属性。

### 5.3 内容与存储位置

首版通过 `AssetLocation` 同时支持托管文件和外部链接文件：

- **托管文件**：默认复制到按账号管理的内容寻址附件库；相同 hash 只保存一份物理文件，便于
  去重、校验、备份和跨设备恢复。
- **链接文件**：保留用户现有磁盘位置，适合已有大量手工目录的用户。
- 一个 `Asset` 在同一账号中最多拥有一个托管位置，可以同时保留多个链接位置。
- 不同账号之间不共享物理去重、托管路径或引用计数。
- 托管附件库使用默认账号目录，也允许用户修改到其他本机固定磁盘位置。迁移必须完成复制、hash 校验、
  切换和失败回滚，不要求用户手工搬移。A1 不把移动磁盘、网络盘或云盘目录作为托管附件库。
- 界面目录是逻辑分类，不要求映射为真实磁盘目录。
- 链接位置同时记录用户选择的原始路径和当时 `realpath` 得到的解析路径。用于实际 I/O 的路径不得先转小写或
  改写 Unicode 形式；大小写折叠与 Unicode 规范化只用于界面候选提示，不作为数据库唯一约束。
- `stat` 提供的设备与文件标识只用于同一次运行中的位置核对，不能替代内容 hash，也不能证明两个路径长期
  指向同一文件。硬链接、符号链接和大小写不敏感文件系统都可能让规范路径不是唯一身份。
- 符号链接、移动磁盘、网络盘和云盘目录可以作为链接来源；断开或不可用时进入缺失状态，首版不承诺持续
  文件监听或网络盘一致性。
- 重新定位支持单文件选择和批量根目录映射。hash 相同时恢复原位置；hash 不同时把所选文件作为
  新 `Asset` 候选，不静默替换原内容。
- 链接文件在原路径发生内容变化时标记为已变更，保留原 `Asset` 记录并让用户确认新内容属于哪个版本。
- “转为托管”和“导出到真实目录”始终先复制再校验。成功后保留原文件，删除某个位置索引由用户
  另行操作。

### 5.4 移除、回收与恢复

界面和服务层明确区分四种操作：

1. **从目录移除**：只删除 `CollectionEntry`。
2. **移除附件**：只解除 `Attachment` 关系；若托管 `Asset` 已无任何附件或导出任务引用，其托管位置进入
   附件回收状态。
3. **将作品移入回收站**：隐藏整个 `Work` 及其版本、附件关系和目录关系，但保留恢复信息。
4. **永久删除**：显示受影响对象，重新检查引用计数，再清理数据库关系和无引用托管文件。

首版回收站不自动过期清理。链接位置在任何删除流程中都不删除用户磁盘上的原文件。恢复作品时一并恢复
可用的关系；若附件已缺失或被永久清理，恢复作品同时显示缺失状态，不把部分恢复报告为完整成功。

### 5.5 文件与数据库一致性

托管导入先进入解析后托管根目录内的临时区，完成流式 SHA-256、复制和校验后再原子切换到内容寻址位置。
临时区与最终对象必须确认处于同一设备；不依赖跨设备 `rename`。若最终 hash 路径已经存在，则校验已有文件并
复用，不用覆盖式 `rename` 替换它。数据库只在文件可用且校验通过后才将托管位置标记为可用。链接导入在正式
入库前完成当次可读性和 hash 校验。

文件占用、只读、权限不足、空间不足和跨设备错误分别保留机器可读原因。Windows 上的打开文件可能因共享模式
拒绝移动或删除，服务层把 `EBUSY`、`EPERM`、`EACCES` 作为可重试或等待用户处理的文件状态，不假设 POSIX
式“删除后打开句柄仍可读”。

`ImportSession` 保留导入阶段和失败原因。取消、超时或崩溃后的对账需覆盖：

- 临时文件已存在但尚未提交：恢复导入或安全清理。
- 内容文件已就位但数据库未提交：按 hash 识别孤立文件并补提交或清理。
- 数据库声称位置可用但文件不存在或校验失败：转为缺失或已变更状态，不静默掩盖。

未完成的导入不作为正式 `Work` 出现在文献库中。

## 6. 阅读上下文与批注图层

### 6.1 目标行为

同一个 `Asset` 在多个目录或研究项目中仍只有一份文件，但可以拥有互不干扰的批注图层。
用户可以选择只看当前图层，也可以叠加查看其他图层。

### 6.2 图层模型

采用“通用批注层 + 多个命名上下文层”：

- 通用层记录与具体项目无关的核心理解。
- 研究项目层记录服务于毕业论文、课程报告或综述的批注。
- 文件夹或项目可以绑定默认图层，但移动目录不能改变批注所有权。
- 阅读器必须始终明显显示当前写入图层，避免批注被写进错误上下文后难以发现。

不推荐把批注严格绑定 `CollectionEntry`。目录重组后批注的研究含义不应随之漂移。

### 6.3 批注定位与版本

- 批注绑定具体 `Asset` 或 `Edition` 版本。
- 定位信息至少保留页码、页面坐标、文本引用和文本上下文指纹。
- PDF 版本变化后不得静默套用旧坐标；可以尝试迁移，但必须展示未匹配项。
- 批注、评论、证据卡片和导出副本之间需要可追溯关系。

## 7. 标签系统

### 7.1 标签不是小色块集合

首版标签系统拥有独立管理页，并覆盖：

- 普通标签绑定 `Work`；版本特有信息保留在 `Edition` 元数据中。
- 规范名称、别名、说明、使用次数和最近使用时间。
- 规范化去除首尾空格并统一 Unicode 形式。大小写、语言、单复数及近似名称只用于重复建议，
  不静默合并用户的可见名称。
- 支持重命名、合并和删除，并在执行前展示受影响论文；合并后旧名称保留为别名。
- 标签删除提供短期撤销，永久删除标签也不删除任何 `Work`。
- 颜色只作为辅助辨识，不作为标签的主要信息结构。

### 7.2 找回体验

- 模糊匹配规范名称和别名。
- 展示最近使用、常用和当前项目使用过的标签。
- 相似标签同时展示说明和代表论文，帮助辨别差异。
- AI 可以推荐已有标签，但默认不得未经确认批量创建新标签。

自动聚类、自动批量整理和复杂标签组合明确后置，不作为首版验收条件。

## 8. 元数据与引用

### 8.1 宽松内部模型

- `Work` 允许没有附件；手工建立的元数据记录、网页、标准和其他研究材料在切片 A2 中可直接保存。
- 类型必须允许 `unknown`、`other` 和用户自定义值。
- 标题、作者、日期、期刊、会议、卷期页码等字段均允许缺失。
- 作者使用可扩展角色，而不是只有固定的作者和编辑。
- 日期可以分别表达发表、上线、接收、修订和访问时间。
- 支持 DOI、ISBN、ISSN、arXiv ID、PMID、URL 等多个标识符。
- 无法映射的外部字段必须保存在原始记录中，不能静默丢失。

### 8.2 字段级来源与冲突

每个关键字段应能够记录来源、获取时间和是否人工确认。多个来源冲突时逐字段比较，不能用
“整条记录覆盖”抹掉人工修正。

默认建议优先级为：

```text
人工确认
  → 精确标识符匹配的外部来源
  → 其他外部来源
  → PDF 内嵌元数据
  → 正文首页识别
  → 文件名
```

优先级只决定默认建议，不代替字段级冲突展示。系统保存必要的外部原始记录、来源、获取时间和解析版本，
不保存凭据或无关响应头。

### 8.3 引用格式是输出层

内部模型不绑定某一种 BibTeX entry type 或 CSL item type。引用导出时再映射到目标格式，
并对无法准确表达的字段给出提示。不得为了兼容引用格式而阻止用户保存不完整材料。

切片 D 的每条外部记录同时保留三层内容：原始记录、解析后的格式影子和映射到 `Work / Edition` 的字段建议。
原始记录用于回看与同格式保真；格式影子保存源格式中的 entry type、tag、key、宏展开结果和未知字段；字段建议
进入现有字段级来源与人工确认流程。CSL JSON 是格式边界之一，不成为新的内部真源。

BibTeX citation key、RIS `ID` 和 CSL JSON `id` 都是来源局部标识。它们用于来源追踪、同一来源文件的再次导入
和优先保留原 key，不作为 `Work`、`Edition` 或跨来源记录的全局身份。跨来源候选仍按规范化 DOI 等标识符以及
标题、作者、年份组合判断；系统不得仅凭相同外部 key 自动合并或覆盖。

### 8.4 首版导入与识别范围

切片 A1 面向用户已经下载到本地的 PDF，不包含浏览器 Connector 或网页内直接抓取。导入时依次利用 PDF
嵌入元数据、正文首页文本、文件名以及 DOI、arXiv ID 等可识别标识符生成元数据建议，并在正式入库前
允许用户确认和修正。

切片 A2 增加无附件 `Work` 和手工元数据记录；BibTeX、RIS、CSL JSON 的具体格式适配仍属于切片 D。

无法可靠识别的信息保持缺失并保留原始内容，不为了补齐字段而编造结果。扫描型或无可靠文本层的 PDF 仍可以
使用文件名、内嵌属性和人工输入入库，并标记正文文本不可用；OCR 属于切片 B。

首次启用外部元数据查询时，界面说明将访问的服务以及会发送的 DOI、标题等信息。默认不上传整份 PDF。离线、
超时或外部服务失败不阻止入库；系统保留未完成查询状态，支持用户手动重试和有上限的后台重试。

已入库元数据不自动定期刷新。用户主动刷新时，新结果仍作为字段级建议，不覆盖已人工确认的值。具体外部
元数据提供方、调用优先级、速率限制和许可条件按以下 A1 选型执行：

1. 本地先用 `pdfjs-dist@6.2.108` 的 Node legacy build 读取 PDF 内嵌信息和正文第一页，只做识别，
   不在 A1 引入阅读器、渲染或 OCR。解析放入可强制终止的隔离任务，关闭脚本求值，限制输入、页数、运行时间
   和返回文本；扫描型、损坏或超时只表示本地识别不可用，不阻止导入。
2. 识别到 arXiv ID 时查询 arXiv API。Atom XML 使用 `fast-xml-parser@5.11.0` 解析并关闭实体处理，
   不用正则表达式解析 XML。
3. 识别到 DOI 时先规范化 DOI，再用 Crossref agency 判断登记机构；Crossref 与 DataCite 的 DOI 分别向对应
   精确接口查询。OpenAlex 的 DOI 精确查询只作为补充候选，不覆盖登记机构结果。
4. 没有可靠标识符时，使用 OpenAlex 的标题、作者、年份检索生成候选，必要时补充 Crossref bibliographic
   候选。标题搜索结果必须在本地按标题、作者和年份重新排序，只供用户选择，不能自动确认作品或版本。
5. A1 不默认接入 Semantic Scholar；其许可条件与当前个人、非商业研究范围绑定，不适合作为基础元数据依赖。

外部查询只发送 DOI、arXiv ID 或必要的标题、作者、年份，不上传 PDF。默认单客户端上限为 Crossref 每秒
4 次且并发 1、DataCite 每秒 1 次、arXiv 每 3 秒 1 次、OpenAlex 每秒 2 次，并继续遵守服务端返回的更严格
限制。单请求默认 8 秒超时，网络错误、`429` 和 `5xx` 最多重试 2 次并遵守 `Retry-After`；普通 `4xx`
不重试。精确标识符的成功结果作为来源记录持久保存，未找到结果缓存 24 小时，瞬时失败缓存 5 分钟。
导入流程不等待后台重试完成。

### 8.5 切片 D 字段映射语义

每条有效外部记录默认映射为一个 `Work` 和零个或一个 `Edition`。只有记录中确实包含期刊、会议、版本、出版者、
完整日期、卷期页码或版本级标识符时才建立 `Edition`；只有标题等作品级信息的记录可以只建立 `Work`。同一文件中的
多条记录互不共享身份，重复候选由现有文献库规则处理。

| 外部内容                                             | 内部目标与处理                                                                                           |
| ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| entry/item type                                      | 映射到最接近的 `Work.type` 与可选 `Edition.kind`；未知值落为 `unknown`，原值留在格式影子与诊断中         |
| title、abstract                                      | 作品级字段建议；空值不覆盖现有值，显式缺失也不解释为删除                                                 |
| author、editor、translator 等姓名                    | 保存角色、顺序、结构化 given/family、literal/机构名和原始写法；无法可靠拆名时保留 literal，不猜测姓与名  |
| issued/published/online/accessed、year/month/day     | 可确定的发表时间进入 `Edition.publishedDate` 与 `Work.year` 建议；其他日期保留类型和原值，不挤入单一年份 |
| journal、booktitle、conference、publisher、volume 等 | 进入 `Edition`；载体标题与作品标题分开，页码范围保留原始表达并可另存规范值                               |
| DOI、arXiv、ISBN、ISSN、PMID、URL                    | 按现有规则规范化并保留原值与来源；未知 identifier 不丢弃，但不伪装成受支持 scheme                        |
| BibTeX key、RIS ID、CSL id                           | 保存为来源局部 key；导出时可作为 citation key 候选，不参与全局实体主键                                   |
| keywords                                             | 只形成标签建议；用户确认后才建立 `WorkTag`，不自动创建目录                                               |
| note、annote、abstract 之外的长文本                  | 留在原始记录和格式影子；不会自动创建 `ResearchNote`、证据或写作正文                                      |
| BibTeX `file`、RIS L1/L2、CSL URL 或附件扩展         | 形成附件候选清单；默认不读取、不复制、不移动，用户逐项选择后才进入 A 的托管或链接生命周期                |
| 宏、未知字段、未知 RIS tag、CSL `custom`/非标准 key  | 保存在格式影子并标记可否同格式重放；跨格式无法表达时进入明确的损失报告                                   |

同一来源再次导入时先比较原始记录摘要、来源局部 key 和已映射实体，只生成字段级差异与重复候选。人工确认字段始终受保护；
即使来源局部 key 相同，也不自动更新已入库记录。用户可以逐字段采用新建议、保留当前值、把记录作为新版本或新作品导入，
也可以跳过该条。一次确认提交只包含用户已选记录和附件，任何失败都不得留下半条 `Work / Edition` 或半个托管文件。

## 9. PDF 阅读器能力底线

第一版可称为“阅读器”之前，至少应覆盖：

- 文档大纲、缩略图、页码跳转、缩放和常用页面布局。
- 全文搜索、文本选择和复制。
- 高亮、下划线、区域标注、评论和书签。
- 批注列表定位、图层切换及当前图层提示。
- 多标签页、最近阅读、阅读位置和缩放状态恢复。
- 单页、连续滚动和双页阅读，以及夜间阅读模式。
- 键盘快捷键和基本无障碍操作。
- 原始文件打开、文件位置查看和带批注副本导出。

手写和触控能力后置评估，不能仅从视觉原型推导。

## 10. 阅读器性能约束

### 10.1 运行策略

- 只渲染视口附近页面，离开缓存窗口的 Canvas 和文本层可回收。
- 后台标签页进入休眠，只保留文档句柄和可序列化阅读状态。
- 页面渲染、解析和索引使用可取消的后台任务，切换文档后停止无用工作。
- 缩略图、正文页面、文本层和搜索索引分别按需加载。
- 使用有明确上限的 LRU 缓存，不随打开文档数量无限增长。
- 派生缓存不在每次启动时全量清空。页面与文本层缓存按休眠和 LRU 上限回收，全文索引与 OCR 缓存按
  `Asset` 内容 hash、引擎版本和语言包版本失效，并提供手动清理入口；临时中间产物在任务结束或重启对账时清理。
- 大型 PDF 使用安全模式，避免打开时一次性解析、渲染和索引全文。
- 文本型 PDF 自动建立全文索引，但索引任务只在后台执行、可以暂停或取消，不能阻塞当前页面。
- 检测到扫描型或缺少可靠文本层的 PDF 时明确提示用户；用户确认后才进入 OCR 流程。
- OCR 通过可替换的引擎接口执行，不在领域模型中绑定具体模型。首版发布前至少完成一个 OCR
  引擎的独立技术验证，再决定默认实现；OCR 结果经校验后进入全文索引。

### 10.2 实施前必须建立的基准

在选定 PDF 引擎和运行形态前，准备小型、扫描型、图片密集型、超长和损坏边缘文档，测量：

- 首次可见页面时间。
- 连续翻页与快速跳页的 CPU 峰值。
- 单文档和多文档峰值内存。
- 后台标签页休眠后的保留内存。
- 关闭文档后的内存回收。
- 全文搜索和索引建立时间。
- OCR 的语言覆盖、准确性、CPU 与内存开销，以及中断后的恢复行为。

首轮 macOS 实测预算见 §17.7。Windows 11 x64 运行同一模块后，以两端较慢结果定稿双平台预算。

## 11. 从阅读到研究知识

### 11.1 导入箱与筛选

每次文件导入都先建立 `ImportSession` 草稿并进入导入箱。单篇导入可以在同一流程中立即确认，批量导入
支持按重复组和问题类型逐组处理。

```text
导入
  → 建立草稿并进入临时区
  → 计算 hash 并检查文件与作品重复项
  → 本地识别与外部元数据查询
  → 确认存储模式、作品归属与元数据
  → 选择目录和已有标签
  → 正式入库
```

作品级候选重复允许用户关联到现有版本、建立新版本、仍创建新作品、暂留导入箱或放弃导入。取消导入时
清理未提交的临时文件；中断后按 §5.5 对账。未确认内容不作为正式 `Work` 进入文献库、目录和标签体系。
研究项目关联在对应上层能力存在时可选显示，不成为切片 A 入库的必要条件。

### 11.2 证据链

首版必须完成以下闭环，而不是只预留数据表：

```text
原文定位
  → 批注
  → 证据卡片
  → 支持 / 反驳 / 限定观点
  → 跨论文对照矩阵
  → 综述或写作材料
```

证据必须保留作品、文件版本、页码、原文、批注图层和提取时间。写作材料应能返回准确出处，
不能退化为与来源脱离的普通文本片段。观点需要能够聚合多篇论文中的支持、反驳和限定证据，
跨论文对照后仍能逐项回到对应原文。

### 11.3 论文项目工作流

可以在 Research Workbench 上层提供研究项目、阶段、里程碑、文献矩阵、方法模型、导师意见和
文稿版本管理。这些是候选能力，不绑定现有视觉原型的布局或固定八阶段流程。

只有“阅读这篇论文”“核实这条证据”“完成综述初稿”等可执行动作才投影为 core `Item`，
论文本身、批注和观点不属于通用待办。

## 12. 检索与重访

### 12.1 切片 A 的结构化检索

可信文献库完成时，搜索和筛选至少覆盖：

- 标题、作者、年份、期刊、标识符和自定义字段。
- 目录、标签、附件角色、文件模式和文件状态。
- 缺失字段、重复项、缺失文件和未完成元数据查询等维护条件。
- 手工建立的相关文献关系。

结构化查询可以保存为智能目录。普通目录保存用户显式添加的 `CollectionEntry`，智能目录保存查询条件，
两者不混用。

### 12.2 后续检索边界

PDF 正文、批注和阅读状态随切片 B 进入检索；笔记、证据和观点随切片 C 进入检索。结构化筛选、模糊搜索和保存
查询应先于语义搜索可靠完成。语义检索可以补充“记得内容但忘了标题”的场景，不成为基础检索的补偿。

## 13. Zotero 优秀能力与互操作边界

### 13.1 不能丢失的基线能力

- 多级集合，以及一篇论文属于多个集合。
- 附件管理、缺失文件提示和重复项识别。
- DOI、ISBN、arXiv 等标识符辅助获取元数据。
- 安全的重复文献合并。
- 全文搜索、保存查询、标签、关联论文、笔记和批注。
- BibTeX、RIS、CSL JSON 等常见格式导入导出。
- 引用复制和基础参考文献生成。

“不能丢失”指产品能力和用户结果，不要求复制 Zotero 的界面、内部数据库结构或全部插件生态。

### 13.2 分阶段互操作

切片 A 负责定义不丢字段的内部导入导出模型、原始记录保存规则、附件清单和可迁移导出包。切片 D 负责把这个
内部模型映射到 BibTeX、RIS、CSL JSON 等具体格式。首版只实现基础互操作；Zotero 专用迁移及后续同步只保留
可扩展接口，不进入首版范围：

1. **首版基础互操作**：BibTeX、RIS、CSL JSON 导入导出，保留外部 key 和原始记录。
2. **后续 Zotero 迁移**：导入条目、集合、标签、附件映射和能够可靠转换的批注。
3. **后续增量互操作**：根据真实需求评估外部 ID、游标和冲突处理。
4. **远期双向同步**：只有删除、版本、附件和批注冲突语义全部确定后才考虑。

不得把直接修改 Zotero 数据库作为基础方案。Connector、Word 插件和完整团队库不属于首轮目标，
但导出接口不能堵死未来接入。

首版定义版本化 `InteropAdapter` 能力契约，能力按 `records / collections / tags / attachment-manifest /
annotations / cursor` 分开声明。切片 D 只实现 `records` 的三格式导入导出；其余能力返回明确的 `unsupported`，
不得用空数组伪装成已经同步完成。契约只交换稳定 ID、来源局部 key、版本、能力和诊断，不暴露 SQLite 表、任意
本地路径或外部服务凭据，也不建立后台同步状态机。

附件互操作只到“候选和清单”边界：解析器可以识别附件字段、URL、MIME、相对路径和显示名，但不能自行访问目标。
预览必须区分远程 URL、本地绝对路径、相对路径、缺失路径和无法识别值。用户明确选择本地文件后，再使用切片 A
已有的文件选择、hash、托管/链接、重复检测和提交回滚流程；导出 BibTeX/RIS/CSL JSON 默认只输出能够表达的
元数据引用，不复制附件，也不把 WorkBench 托管根绝对路径泄露到文件中。需要附件文件的完整迁移继续使用 A 的
规范迁移包。

## 14. 存储、备份与同步边界

- PDF 不作为 SQLite BLOB 保存；数据库只保存文件标识、路径、hash、大小、MIME、版本和状态。
- 当前账号数据库备份可以覆盖研究元数据、阅读状态和旁挂批注，但不会自动携带 PDF 附件。
- 备份和恢复界面明确区分“数据库备份”与“完整资料导出”，不用笼统的“备份成功”掩盖附件未包含。
- 切片 A2 的可迁移导出包包含规范 JSON、元数据、目录、标签、作品关系、来源记录和附件清单。用户可选把托管文件
  复制进导出包。
- 链接文件默认只输出位置和状态清单并报告缺失项；只有用户明确选择时，才复制当前可访问的链接文件。
- 附件备份、去重上传、恢复校验、配额和可选加密需要单独设计。
- 数据库恢复与附件恢复必须检测缺失和版本不一致，不能显示“恢复成功”却留下静默断链。
- 当前云端“保险箱”模型不等于多设备并发同步；研究模块不得暗中假设实时协作成立。

## 15. 仓库边界

Research Workbench 应作为独立领域进入 `modules/research`，遵守现有模块边界：

- 模块只能依赖 core，模块之间零依赖。
- core 不感知论文、PDF、Zotero、批注或研究项目。
- 模块拥有自己的迁移、存储适配器、契约、服务端注册和 UI 注册。
- 与应用壳层的前后端交接继续通过模块 `contract.ts` 和注册点完成。
- Todo、工作台与日历不直接依赖 research；research 只把可执行动作投影为 core `Item`。

可能的内部目录如下，仅表达职责，不是实施文件清单：

```text
modules/research
├─ library       作品、版本、集合和标签
├─ storage       文件索引、去重、回收站和附件状态
├─ ingest        导入箱、元数据识别和重复检测
├─ reader        PDF 阅读状态与性能策略
├─ annotation    批注图层、书签和笔记
├─ knowledge     证据、观点和综述矩阵
└─ interop       Zotero、BibTeX、RIS、CSL JSON
```

A1 的 PDF 识别与外部元数据客户端先保留在 research 模块内部，通过窄接口隔离解析任务和远程提供方。
只有切片 B 的阅读器或其他模块出现真实复用需求时再提取独立 package。

### 15.1 内置 AI Runtime 与 Harness 接入预留

未来的 Harness 类能力计划作为 Personal WorkBench 内置的 AI Runtime 运行，而不是以一个外部
系统直接控制 Research Workbench。Research Workbench 的核心管理、检索和阅读能力仍不能依赖
AI 服务才能运行。

- 内置 AI Runtime 属于应用级基础能力，负责模型调用、会话与任务生命周期、流式输出、取消、
  能力编排、用户确认、凭据和审计；它不成为 research 领域数据的真源。
- `modules/research/src/contract.ts` 应提供稳定、版本化、可结构化验证的领域能力与数据契约，
  由应用装配层注册给内置 Runtime。不能因此让 research 依赖其他业务模块，也不能让 core 感知
  论文或 AI 领域。
- research 模块仍负责领域输入验证、权限判断和持久化语义；Runtime 只能通过已注册能力操作数据，
  不能绕过模块服务直接读写数据库。
- Runtime 设计时兼顾结构化请求与结果、流式增量、可取消任务、工具或能力调用、机器可读错误、
  请求关联以及来源追踪等常见接口形态。
- **MCP 作为首选兼容目标**：内部能力注册应能自然映射为 MCP 的 tools、resources 等概念，
  首个通用接入适配器优先考虑 MCP；但同进程模块之间不必为了兼容而强制经过 MCP 传输。
- Anthropic Messages API、OpenAI Responses API 等属于模型供应商适配层，可以按实际使用的模型
  分别支持，不作为 WorkBench 的固定内部 API 格式。具体字段在 Runtime 项目需求明确后再确定。
- 模型接入优先兼容 CC Switch 管理的配置，同时支持用户直接配置自己的 API Key（BYOK）；具体
  集成方式留待 Runtime 设计时确定。
- 接口优先传递 `Work`、`Edition`、`Asset`、`ReadingContext` 等稳定资源标识，不直接暴露内部
  数据库结构、任意磁盘路径或默认传输整份 PDF。
- AI 生成的标签、元数据、证据和观点默认属于待确认建议；是否持久化以及如何覆盖既有内容仍由
  WorkBench 的权限、验证和确认流程决定。
- 凭据、隐私边界、远程发送范围和生成结果来源必须可审计，不能把模型密钥或供应商响应对象
  混入研究领域数据模型。

本节只保留未来兼容方向，不要求在当前阶段确定完整 API schema，也不阻塞任何非 AI 切片。

## 16. 已确认的产品切片与首版范围

以下仅用于约束依赖顺序，不是 implementation plan。每个切片拥有自己的开工门槛；后续切片的
未决问题不能反向阻塞已经具备独立闭环的前置切片。

首版产品范围包含切片 A、B、C，并按 A → B → C 的顺序形成可验证闭环。切片 D 在首版只完成
BibTeX、RIS、CSL JSON 基础互操作以及未来扩展所需的接口边界。

A、B、C、D 是产品能力切片。由于切片 A 内部包含多组复杂且高风险语义，它被拆成两个可独立验收的子里程碑：
A1 和 A2。它们不与 A、B、C 平级，共同完成切片 A。

正式实现 A1 之前先确认完整切片 A 的领域身份、数据归属、文件生命周期、后续管理能力和迁移出口，
避免 A2 反过来要求 A1 迁移核心数据。实施顺序仍为 A1 → A2；A2 的逐文件实施计划在 A1 闭环验证后再编写。

### 切片 A：可信文献库

完成作品、版本、文件、目录、标签、导入箱、元数据来源和可迁移导出，先证明不会丢文件和字段。

切片 A 采用托管与链接并存的文件模式以及安全删除与恢复语义，不依赖完整 PDF 阅读器引擎和
阅读性能验证即可先行实施；A1 仍需支持用于元数据识别的基础 PDF 文本与内嵌信息提取。
PDF 识别与外部元数据提供方采用 §8.4 的已验证选型。

#### A1：论文导入与安全存储闭环

A1 先证明论文从本地 PDF 进入 WorkBench 后，能够被稳定识别、去重、分类、检查并安全恢复。
第一个用户可验收闭环是：

```text
导入本地 PDF
  → 选择托管或链接
  → 计算内容 hash
  → 本地自动元数据识别与外部元数据查询
  → 用户确认或修正元数据
  → 创建或复用 Work / Edition / Asset / Attachment
  → 在最小文献库中显示
  → 添加到多个目录
  → 安全移除、缺失检测、重新定位与恢复
```

A1 当前已确认的范围是：

- 建立 `Work`、`Edition`、`Asset`、`AssetLocation`、`Attachment`、`Collection`、`CollectionEntry`、
  `ImportSession` 和 `MergeRecord` 的最小稳定身份与关系。
- 同时支持托管和链接位置，保留内容 hash、大小、MIME、已知路径、位置模式和状态。
- 对 PDF 内嵌元数据、正文首页、文件名和可识别标识符进行本地自动识别，并通过外部元数据服务生成
  补全或冲突建议；所有建议均在正式入库前交由用户确认。
- 保留字段来源、获取时间和人工确认状态；外部结果不得静默覆盖用户已确认的值。
- 按 hash 对 `Asset` 精确去重。已经存在且作品归属明确的同一文件再次导入时，复用现有对象并只增加
  需要的 `CollectionEntry`，不静默新建一套 `Work / Edition / Attachment`；归属不明时交由用户确认。
- 对 hash 不同但 DOI 相同或标题、作者、年份高度近似的文件只提示作品级候选重复，不自动合并或
  丢弃版本。
- 提供最小 UI：导入 PDF、选择存储模式、确认元数据、查看文献库、多目录归属和文件状态。
- 通过自动测试覆盖精确重复、候选重复、目录引用、托管与链接删除、缺失、重新定位、回收、恢复和中断对账。

A1 不包含 PDF 阅读器、OCR、AI、批注、证据链、完整标签治理和完整引用格式导入导出，只为它们保留
稳定资源 ID 与模块契约边界。

#### A2：完整文献库治理与迁移出口

A2 在 A1 的可信身份和文件基础上完成切片 A 的其余能力：

- 完整导入箱和批量筛选，包括按重复组、缺失字段和失败原因处理。
- 无附件 `Work`、手工元数据记录、通用附件角色和手工相关文献关系。
- 完整层级目录、多目录归属、批量操作、系统视图和保存查询形成的智能目录。
- 标签管理页、规范名称、别名、近似检测、重命名、合并、删除和撤销。
- 覆盖元数据、目录、标签、文件与维护状态的结构化搜索、模糊搜索和保存查询。
- 规范 JSON、原始来源记录、附件清单和可选附件文件组成的可迁移导出包。
- 导入箱、全部文献、目录、标签、重复项、缺失文件和回收站的完整管理界面。

A2 不实现 PDF 正文搜索、批注、OCR、证据链或具体引用格式适配器。PDF 正文与批注属于 B/C，BibTeX、RIS、
CSL JSON 适配属于 D。

### 切片 B：可长期使用的阅读器

完成基本阅读、阅读状态、批注图层和大型文档性能基准，证明连续使用不会失控占用资源。

切片 B 继续作为一个完整切片，只编写一份实施计划，内部按以下阶段连续推进：

- **B0 技术验证**：准备性能语料，验证 PDF.js、受控 Range 请求、资源回收、全文索引和 OCR，形成实测预算。
- **B1 阅读器核心**：完成页面渲染、虚拟化、标签页、阅读状态、后台休眠和资源回收。
- **B2 批注与检索**：完成批注图层、文本与区域标注、每页全文索引、搜索和定位。
- **B3 OCR 与输出**：完成用户确认后启动的 OCR、带批注副本导出和切片最终验收。

#### B 的技术方向

- 阅读器直接使用 `pdfjs-dist@6.2.108`，不通过 React-PDF 包装。页面虚拟化、缓存上限、任务取消和
  后台休眠由 Research 模块控制。
- 本地 server 通过受控路由提供 PDF 内容和 HTTP Range；浏览器中的 PDF.js worker 负责解析与渲染。
  阅读器不直接暴露任意本地文件路径。
- OCR 首选候选为 Tesseract.js，首轮验证英语和简体中文。OCR 只在用户确认后本地运行，支持取消和恢复，
  并通过可替换接口接入；若实测不能满足需求，再比较原生或 Paddle 系列方案。
- 文本型 PDF 在首次打开后按页惰性建立 SQLite FTS5 索引，空闲时继续。索引是可取消、可恢复、可重建的
  派生数据，不与原始 PDF 或批注一起成为唯一真源。
- 加密 PDF 的密码只保存在当前阅读会话内，不写入数据库、日志或配置。
- 原始 PDF 保持不可变。带批注导出始终创建新文件；能可靠表达的批注写为标准 PDF 批注，其他类型扁平化，
  并生成导出报告说明处理结果。
- 带批注导出锁定 `pdf-lib@1.17.1`（MIT）。它读取原 PDF 后生成完整重写副本，不提供增量保存；高亮、下划线、
  删除线、区域和便笺使用标准 annotation，页书签扁平化到页面边缘。输出先写入目标同目录临时文件，完成 hash、
  页数和 PDF.js 可读性检查后原子发布，绝不覆盖原始 Asset。

#### B 的阅读与批注语义

- 每个 `Asset` 始终有通用批注层。命名上下文由用户手工创建，可选择绑定目录；从已绑定目录打开时默认选择
  对应上下文，其他入口使用该文档上次选择的上下文，系统不自动创建命名上下文。
- 阅读位置、缩放和布局按“账号 + `Asset`”保存；批注按“`Asset` + 上下文”保存。批注锚点同时保留页码、
  PDF 页面坐标或四边形、文本引用、周边文本指纹和 `Edition`，版本变化后不得静默套用旧坐标。
- 批注使用稳定 ID，保留轻量修订快照；删除写入 tombstone 并允许撤销。上下文切换只改变当前写入和显示范围，
  不移动或复制原有批注。
- 切片 B 负责 PDF 正文检索、阅读状态和批注。证据卡片、观点、跨论文矩阵及其来源回跳属于切片 C，
  不进入 B 的数据模型和界面。

#### B 的界面与验收范围

- 本切片可以重构 Research 文献库和阅读器界面，不改造 WorkBench 全局外壳及其他模块界面。
- 自动视觉验收在 1440、1024、768 和 390 像素宽度分别使用全新页面状态截图；390 像素下侧栏和属性面板
  可以改为抽屉。键盘操作、文本选择、批注、标签页切换和资源回收另做真实浏览器手动验收。
- 性能基准同时覆盖 macOS arm64 和 Windows 11 x64，预算以两台目标设备中较慢的实测结果为依据。
  语料由生成样本、公开许可 PDF 和仅在本机使用的私有 PDF 组成；私有文件及其可识别路径不得提交到仓库。

以上方向已经确认。B0–B3 已完成实现和 macOS 验收；Windows 结果在对应模块声明双平台兼容前补齐。

### 切片 C：研究知识闭环

完整实现批注、证据、观点、跨论文对照和来源回跳，让阅读结果能够进入综述与写作。这里的
“完整”指 §11.2 的研究知识闭环，不要求同时实现 §11.3 中全部候选项目管理能力。

切片 C 不另设独立技术验证阶段。它继续使用 B 已验证的批注锚点、PDF/OCR 来源定位、SQLite FTS5 和安全文件输出，
没有需要先行决定架构的新引擎。实施仍作为一个完整切片编写一份计划，内部按 C1、C2、C3 连续推进。

#### C 的完整用户流程

```text
阅读 PDF 并选择原文或区域
  → 创建或复用一条可见批注
  → 提炼证据并保存来源快照
  → 在研究上下文中建立观点
  → 用支持 / 反驳 / 限定关系连接证据
  → 将多篇作品放入跨论文对照矩阵
  → 整理为轻量写作材料
  → 从证据、矩阵或写作材料回到原始页码和位置
```

用户也可以先建立研究笔记或草稿观点，再补充证据。缺少证据的观点必须明确显示为草稿，不能伪装成已经得到
文献支持的结论。

#### C 的归属与复用

- 现有 `ReadingContext` 继续作为研究知识的工作空间，不为 C 新建 `ResearchProject`。通用层和命名上下文都可以
  拥有笔记、证据、观点、矩阵和写作材料。
- `ResearchNote`、`Evidence`、`Claim`、`ComparisonMatrix` 和 `WritingDocument` 各自只属于一个当前上下文。
  上下文归属用于确定默认工作区和检索范围，不随目录移动而改变。
- `Evidence` 的来源快照另行保存创建时的来源上下文。证据可以被其他上下文中的观点、矩阵和写作材料引用；
  复用只建立新关系，不复制证据或来源快照，界面始终显示证据的当前归属与创建来源。
- 完整论文项目、阶段、里程碑、导师意见和文稿版本管理继续属于 §11.3 的候选能力，不成为 C 的容器或验收前提。

#### 笔记、证据与观点

`ResearchNote` 是上下文内的轻量自由笔记，保存标题和正文，并可关联 `Work`、`Annotation`、`Evidence` 和
`Claim`。它补充批注正文无法承担的跨来源整理，不扩展为任意页面层级、双向链接图谱或通用知识库。

每张 `Evidence` 必须来自一条批注。用户从当前文本选择或页面区域直接创建证据时，系统在同一事务中先建立一条
正常可见的批注，再建立证据；事务任一步失败都不得留下半条证据链。文本证据保留准确原文，区域、图表和整页来源
可以没有文本引用，但必须填写证据说明。证据来源可以来自 PDF 文本层或 OCR，来源类型不能丢失。

证据同时保存当前来源链接和创建时的来源快照。快照至少包含：

- `Work`、`Edition`、`Asset`、批注和阅读上下文的稳定 ID；
- PDF 页码、页面尺寸、矩形或四边形、原文与周边文本指纹；
- PDF 文本或 OCR 来源类型、批注 revision、Asset hash 和提取时间。

证据标题、摘要和研究说明可以编辑，来源快照不会随批注或 OCR 索引变化而静默改写。批注修订、删除、PDF 版本变化、
OCR 重建或文件不可用时，证据分别显示来源已修订、来源已删除、版本不匹配或来源不可用。用户可以回看原快照，或在
预览差异后显式重新绑定；重新绑定产生证据 revision，旧快照仍可追溯。

`Claim` 保存观点正文和可选说明，状态为草稿、使用中或已归档。观点可以在没有证据时创建，但界面必须持续显示
“尚无证据”。`ClaimEvidence` 是多对多关系，关系类型固定为支持、反驳或限定，并可保存简短的关系说明。同一证据
可以服务多个观点和上下文；删除关系只解除连接，不删除证据或观点。首版不实现观点树、子观点或自动逻辑推导。

#### 跨论文对照矩阵

`ComparisonMatrix` 属于一个上下文，列是有稳定 ID 的 `Work`，行分为两类：

1. 关联一个现有 `Claim` 的观点行；
2. 用户填写标题或问题的比较维度行，例如样本、方法、主要结论和限制。

单元格由人工综合说明和显式选择的证据链接组成。系统可以根据行关联的观点、列对应的作品和现有证据关系归集候选
证据，但不自动生成综合说明。证据、观点或来源 revision 变化后，受影响单元格标记为需要复核；只有用户重新检查并
确认后才更新复核基线。矩阵不是通用电子表格，不提供任意公式、跨表计算或无研究语义的自由行列类型。

#### 写作材料、导出与检索

`WritingDocument` 是上下文内的轻量写作板，包含标题、按顺序排列的章节，以及普通文本、笔记引用、证据引用、
观点引用和矩阵引用块。用户可以编辑衔接文字和调整块顺序；引用块保留稳定资源 ID，不能粘贴成失去来源关系的普通
文本。C 不实现完整富文本排版、协作审阅或文稿版本管理。

矩阵可以导出 Markdown 和 CSV，写作板可以复制或导出 Markdown。人类可读来源标记至少包含作品标题和页码，
同时写入稳定资源 ID 或内部回跳地址；规范 JSON 迁移包覆盖全部 C 数据及 revision。BibTeX、RIS、CSL JSON、
正式引用样式和参考文献表继续由切片 D 负责。

`ResearchNote`、`Evidence`、`Claim` 和写作板正文进入 SQLite FTS5。检索可以按上下文、作品、对象类型、状态和
来源可用性筛选，并从结果回到对应知识对象或原文位置。首版不以语义搜索或 AI 摘要代替结构化检索与全文检索。

#### 编辑、删除、归档与上游生命周期

- 笔记、证据、观点、矩阵、单元格综合说明和写作板采用 revision 乐观并发。更新前保留轻量快照；删除写入
  tombstone 并可恢复，不立即硬删除。
- 关系拥有独立身份。解除资源关联、移除观点证据关系、移除矩阵证据或移除写作引用块，都不级联删除两端对象。
- 归档仍保留可读、可检索和可导出的内容。删除对象恢复后，原关系在两端均可用时恢复显示；任一端仍删除或不可用时
  显示缺失关系，不伪造完整恢复。
- 命名上下文含有 C 数据时，归档操作必须预览批注、笔记、证据、观点、矩阵和写作板的影响。保留归档会冻结整套
  知识内容；迁移到通用层或另一上下文时移动完整知识归属和当前关系，不能只移动批注。创建时的来源快照保持不变。
- 有效证据构成对来源 `Work / Edition / Asset / Annotation` 的知识引用。永久删除作品、附件或无引用托管文件前，
  影响预览必须包含知识链并阻止意外清理；用户先删除或重新绑定相关证据后才能继续。
- 作品合并把当前来源关系重定向到幸存 `Work`，同时保留证据创建时的原作品 ID、版本和来源快照。作品进入回收站
  不删除知识对象，来源回跳明确显示作品状态。

#### C 的界面和实施阶段

Research 模块内部增加“文献库 / 研究知识”导航。阅读器提供提炼证据、查看来源状态和回跳入口；独立研究知识页面
提供笔记、证据箱、观点、矩阵和写作板。跨论文工作不塞入单篇 PDF 的阅读器侧栏，也不创建新的 WorkBench 顶层模块。

切片 C 的一份实施计划按以下阶段连续推进：

- **C1 来源与证据**：完成研究笔记、证据、来源快照、变化检测、重新绑定和来源回跳。
- **C2 观点与对照**：完成观点、支持/反驳/限定关系、候选证据归集和跨论文矩阵。
- **C3 写作与交付**：完成写作板、C 对象检索、Markdown/CSV/JSON 输出和切片最终验收。

核心流程不接入 AI。未来 AI Runtime 只能生成待确认建议，并通过稳定的 research 能力契约提交，不能绕过来源、
revision 和用户确认语义。

### 切片 D：生态兼容与附件保护

切片 D 把已经可信的文献库接到常见引用生态。首版实现 BibTeX、RIS、CSL JSON 基础导入导出、citation key、
APA/IEEE/Chicago author-date 引用复制和参考文献表，并保留版本化扩展接口。不实现完整 Zotero 迁移、附件云备份、
后台同步、Word 插件或浏览器 Connector。

#### D 的完整用户流程

导入继续使用现有导入箱，但格式记录与 PDF 项目分别展示，不把元数据文件伪装成附件：

```text
选择 BibTeX / RIS / CSL JSON 文件
  → 识别编码与格式并拆分记录
  → 逐条解析，保留原始记录、格式影子、未知字段和错误位置
  → 映射为 Work / 可选 Edition 的字段建议
  → 显示重复候选、字段冲突、标签建议和附件候选
  → 用户逐条、逐字段确认，并对附件明确选择忽略 / 托管 / 链接
  → 在一个可回滚提交中创建或复用 Work / Edition 与来源记录
  → 返回已导入、已跳过、需复核和失败的逐条报告
```

部分无效文件不会整体丢弃。能够确定边界且成功解析的记录继续进入预览；无效记录保留原文、条目序号、行号范围、
错误和可重试状态。若文件本身无法确定记录边界或 CSL JSON 顶层结构损坏，则停止正式提交并显示文件级错误，不能
猜测修复后静默入库。

导出和引用输出使用当前文献库状态，不直接重放旧来源记录：

```text
选择当前勾选 / 当前目录 / 当前筛选 / 全部有效作品
  → 默认每个 Work 选择 preferred Edition，可显式改为全部 Edition
  → 生成 citation key、目标格式字段和逐条损失诊断
  → 预览数量、重复 key、缺失关键字段、附件边界与覆盖目标
  → 用户确认
  → 同目录临时文件写入、编码与记录数校验、原子发布
  → 返回成功项、降级字段、未映射字段和输出摘要
```

回收站作品默认不参与导出。当前筛选在创建预览时冻结为稳定 ID 清单；预览后对象 revision 变化、作品被回收或
preferred Edition 改变时，提交拒绝并要求刷新预览，不能悄悄导出另一批内容。

#### 导入冲突与来源保护

- 每条导入记录先保存来源文件摘要、记录序号、原始内容摘要、格式、解析器版本和来源局部 key。来源文件路径只作为
  当前本地观察值，不构成稳定身份；移动文件不会创造新的全局记录身份。
- DOI 等精确标识符、标题/作者/年份相似度和现有来源映射只产生“现有作品 / 新版本 / 新作品”的候选。系统默认不
  自动合并；同一来源局部 key 也不例外。
- 字段审查以当前选中值、导入建议和来源原值三列显示。用户可以逐字段采用或保留，并明确选择是否把该选择标记为
  人工确认。已经人工确认的字段不会因批量采用、再次导入或解析器升级而自动改变。
- 一条记录映射到现有 `Work` 时，可以新建 `Edition` 或只补充未选中的来源建议。若选择现有 Edition，更新仍需
  revision 检查；冲突返回最新值和原选择，不进行最后写入者覆盖。
- 重复来源记录、相同 DOI 不同文件、相同标题不同年份、相同 key 不同内容和同一记录多次导入均必须可区分。提交
  报告记录最终动作，不用一个笼统“导入成功”覆盖跳过与冲突。

#### 同格式保真与跨格式损失

无字段变更的同格式导出优先重放已保存的原始记录，保持未知字段、未知 RIS tag、BibTeX entry type、源 key 和
CSL 非标准键。已修改记录以当前内部值覆盖对应已知字段，同时从格式影子合并不冲突的未知字段；不能安全合并的值
留在导出报告，不静默丢弃，也不能用旧原文覆盖用户新值。

跨格式导出只保证三种格式共同能表达的规范语义。每条记录的报告至少区分：完整映射、规范化、降级、未映射、
附件未输出和无可用 Edition。用户可以在预览中查看原字段、目标字段和原因；报告可以复制或随导出另存 JSON，
但默认不额外创建报告文件。

#### citation key 与 CSL 引用输出

- 已导入且不冲突的 BibTeX key 优先沿用。其他记录按规范化 `author + year + title` 生成稳定、可编辑的基础 key；
  缺少作者或年份时使用明确的占位组成，不从随机数或数据库行号生成。
- key 在当前导出集合内大小写归一后判重，碰撞按稳定排序添加 `a / b / c` 后缀；再次以相同作品、版本、字段和排序
  导出时结果一致。用户编辑 key 后立即重新验证空值、非法字符和冲突。
- citation key 是导出层结果，不回写 `Work.id`。用户可以保存 key 偏好；删除、合并或更换 preferred Edition 后
  重新预览并说明变化。
- CSL 引擎通过独立适配器接收 CSL JSON 投影和固定版本的 style/locale。首版内置 APA、IEEE 和 Chicago
  author-date，输出文内引用与参考文献表的纯文本、Markdown 和经过净化的 HTML。Markdown 由受控转换器从 CSL
  结果生成，不能把任意 HTML 原样塞入编辑器。
- 文献库选择区提供复制引用和生成参考文献表；C 的写作板可以插入带稳定 `Work / Edition` ID 的引用块并生成
  当前草稿参考文献表。生成结果是可刷新输出，不能把来源关系替换成不可追踪的普通字符串。

#### D 的实施阶段

切片 D 只形成一份实施计划，并在计划确认后依次推进：

- **D1 格式导入与字段审查**：完成格式识别、记录边界、原始/格式影子保存、映射诊断、局部错误、重复与字段冲突
  预览、附件候选和确认提交。
- **D2 格式导出、citation key 与 CSL 引用**：完成四类选择范围、preferred/all Editions、三格式安全输出、
  citation key、三种样式和三种复制表示，并接入文献库与写作板。
- **D3 扩展接口和最终验收**：完成版本化 `InteropAdapter` 能力契约、记录适配器、迁移/回归、10,000 条规模、
  文件输出与浏览器验收；其他能力只声明边界，不实现同步。

D0 已完成并用于确定解析和 CSL 技术边界；D1–D3 的实现必须等待唯一实施计划确认。

D3 将规范迁移格式升级为 canonical v3，纳入 interop source、record、record-to-entity 映射和 citation key 偏好。
导入/导出任务、预览、进度、CSL 渲染结果和缓存仍是可重建状态，不进入 canonical。v1/v2 文件按空 D 集合升级后
继续可读，不改写原文件。

## 17. 关键验收场景与测试策略

在为某个子里程碑或切片编写实施计划前，先把属于它的场景改写为可直接转成自动测试或手动验收的标准。

### 17.1 切片 A 功能验收

1. 同一 PDF 以托管模式导入两次，只保留一个 `Asset` 和一份托管内容；重复导入可增加新目录引用。
2. 同一 `Asset` 可以同时保留托管位置和多个链接位置；不同账号之间不共享物理文件或引用计数。
3. 两份字节不同但 DOI 相同或元数据高度近似的 PDF 进入候选重复；用户可选现有版本、新版本、
   新作品、暂留或放弃，系统不自动合并。
4. `unknown` 类型、不完整元数据、无附件 `Work` 和扫描型 PDF 都可以保存、搜索、编辑和无损导出。
5. 外部元数据服务离线或失败时仍可入库；恢复后可重试；新结果不覆盖人工确认字段，原始来源可追溯。
6. 取消或中断导入不会在文献库留下半成品 `Work`；重启对账能处理临时文件、孤立托管内容和断链记录。
7. 链接文件移动后标记为缺失。重新定位到同 hash 文件时恢复；所选文件 hash 不同时进入新 `Asset`
   候选，不替换原内容。
8. 删除一个目录或一个 `CollectionEntry` 不会删除其他目录中的作品、附件关系或物理文件。
9. 移除目录引用、移除附件、回收作品和永久删除是四个独立操作；无引用托管文件经回收与二次检查后才清理，
   链接原文件永不由 WorkBench 删除。
10. 恢复作品时恢复仍可用的关系；附件缺失时明确报告部分恢复。回收站不自动清空。
11. 作品合并保留原 ID 重定向、来源映射和可撤销记录；永久清理前可以恢复。
12. 近似标签只提示不自动合并；标签合并保留旧名为别名，标签删除和撤销不丢失作品。
13. 结构化检索覆盖元数据、目录、标签、文件状态和维护条件；保存查询可作为智能目录重复执行。
14. 可迁移导出包可还原元数据、目录、标签、作品关系、来源和附件清单；托管与链接文件按用户选项复制，
    缺失项单独报告。
15. 托管附件库迁移只在复制和 hash 校验完成后切换；中断时继续使用原位置并可重试。

### 17.2 切片 A 规模与资源测试

切片 A 的基线资料库规模为 10,000 个 `Work`、20,000 个 `Asset` 索引、1,000 个标签和单次 200 个文件的
批量导入。数量规模与真实 PDF 容量分层验证，不为了测试记录数量复制等量大型 PDF。

| 测试层       | 方法                                                             | 默认资源边界           |
| ------------ | ---------------------------------------------------------------- | ---------------------- |
| 数据规模     | 临时磁盘 SQLite 直接生成领域记录，测试索引、分页、搜索和批量操作 | 不生成等量 PDF         |
| 文件生命周期 | 30–50 个小型真实 PDF，覆盖去重、链接、缺失、回收、恢复和中断     | 进入默认测试套件       |
| 批量导入     | 200 个小型生成文件，验证流程数量、候选分组、取消和失败恢复       | 进入专项测试           |
| 大文件行为   | 单个约 256 MiB 生成文件，验证流式 hash、复制进度、取消和清理     | opt-in，不进入每次提交 |

默认测试套件的临时磁盘占用上限为 250 MiB，用例结束后清理独立临时目录。数据规模测试使用临时磁盘数据库，
不用 `:memory:` 代替需要观察的真实 SQLite 索引、查询和 WAL 行为。磁盘不足通过可控文件系统边界注入错误，不实际写满磁盘。

20,000 个真实目录项的文件库审计和更大文件测试作为手动或夜间任务，并允许通过配置把临时根目录指向外接磁盘。

首次 SQLite 原型基准已给出以下 A1 验收阈值。查询阈值不包含 PDF 解析、文件复制、hash 和网络等待；正式表结构、
索引和 Repository 完成后使用相同数据分布重跑，不能用原型结果代替实现验收。

| 指标                                          | A1 验收阈值               |
| --------------------------------------------- | ------------------------- |
| 普通列表、分页、精确筛选和元数据搜索 p95      | 不超过 100 ms             |
| 缺失审计、重复分组和导入对账查询 p95          | 不超过 250 ms             |
| 200 条纯元数据在单事务中的提交时间            | 不超过 1 s                |
| 1,000 次精确内容 hash 查询                    | 总计不超过 100 ms         |
| 10,000 `Work` / 20,000 `Asset` 索引数据库大小 | 不超过 100 MiB            |
| 上述基准进程峰值 RSS                          | 不超过 250 MiB            |
| 同规模数据库 `integrity_check`                | 不超过 2 s                |
| 默认自动测试的全部临时磁盘占用                | 不超过 250 MiB 且结束清理 |

### 17.3 A1 精确验收

| 编号  | 输入与操作                                                           | 必须观察到的结果                                                                                                      |
| ----- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| A1-01 | 首次以托管模式导入一个正常 PDF                                       | 一个正式 `Work`、`Edition`、`Asset`、`Attachment` 和可用托管 `AssetLocation`；对象路径与 SHA-256 一致，源文件不被修改 |
| A1-02 | 同一 PDF 再导入一次，并用两个并发导入模拟竞争                        | 仍只有一个 `Asset` 和一份托管内容；已有作品归属明确时不新增 `Work`、`Edition` 或 `Attachment`，导入结果均可追溯       |
| A1-03 | 以链接模式导入普通路径和符号链接路径                                 | 不复制源文件；保存原始路径、解析路径、hash 与状态；两条位置可以引用同一 `Asset`                                       |
| A1-04 | 导入 hash 不同但 DOI 相同的两个 PDF                                  | 保留两个 `Asset`，显示现有版本、新版本、新作品、暂留或放弃选项；用户确认前不自动合并                                  |
| A1-05 | PDF 内嵌字段、首页识别和外部结果互相冲突，然后人工确认一个字段       | 逐字段展示来源；人工值入库后，重试和刷新都只产生建议，不覆盖人工值；原始来源和获取时间可查                            |
| A1-06 | 断网、超时、`429`、服务端错误和无匹配结果分别发生                    | 导入仍能完成；状态、退避和缓存符合 §8.4；恢复网络后可手动重试，不重复轰击服务                                         |
| A1-07 | 同一作品加入两个目录，再移除其中一个目录引用                         | 另一个目录引用、作品、附件和物理文件均保留；数据库中不产生第二份文件内容                                              |
| A1-08 | 移走链接文件，再分别选择同 hash 文件和不同 hash 文件重新定位         | 前者恢复原位置，后者进入新 `Asset` 候选；系统不把不同内容静默替换为原 `Asset`                                         |
| A1-09 | 分别执行移除目录引用、移除附件、作品回收、作品恢复和永久删除         | 四类动作影响范围符合 §5.4；链接源文件始终存在；托管文件只有在永久清理前再次确认无引用才删除                           |
| A1-10 | 在复制中、文件就位后数据库提交前、数据库提交后状态更新前分别中断进程 | 重启对账后没有半成品正式 `Work`；临时文件、孤立对象和错误位置均进入可解释且可重试或可清理的状态                       |
| A1-11 | 导入文件名混乱、不完整元数据、扫描型、损坏、只读和超大文件           | hash 与安全存储仍可完成；本地识别失败时明确标记并允许人工确认，不因 PDF 解析失败丢文件或留下临时内容                  |

### 17.4 A1 技术验证记录

2026-08-23 在 macOS APFS、Node.js 22.22.1 和仓库现有 SQLite 栈上完成首轮验证：

| 验证项             | 实测结果与结论                                                                                                                                                                                                                                                    |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SHA-256 与托管复制 | 256 MiB 生成文件两次流式 hash 分别约 1.26 s 和 1.38 s，吞吐约 203 MiB/s；流式复制约 1.42 s，目标 hash 一致。约 16 MiB 时取消返回 `ABORT_ERR`，残留临时文件可清理。采用 Node 内置 `crypto`，不增加 BLAKE3 依赖。                                                   |
| PDF 基础识别       | `pdfjs-dist@6.2.108` legacy build 可从带内嵌字段 PDF 和论文样本提取标题、作者、关键词、首页文本、DOI 与 arXiv ID；扫描型返回无文本，截断文件返回明确的无效 PDF 错误。现代 Node build 在无 DOM 环境缺少 `DOMMatrix`，A1 不使用它。                                 |
| arXiv XML          | `fast-xml-parser@5.11.0` 在 Node.js 22 下成功解析官方 Atom 响应；关闭实体处理后，自定义实体保持未展开。该依赖为 MIT，安装体积约 1.3 MiB。                                                                                                                         |
| 外部元数据         | Crossref、DataCite、arXiv 和 OpenAlex 的精确标识符查询均返回有效记录。标题检索出现同名后发论文，Mozilla 样本还出现会议版与期刊载体两个有效 DOI，确认标题检索只能生成候选，`Work / Edition` 不能合并。                                                             |
| SQLite 目标规模    | 10,000 `Work`、20,000 `Edition / Asset / Location / Attachment`、20,000 目录引用、1,000 标签和 30,000 作品标签在约 0.53 s 内生成；数据库约 24.97 MiB，峰值 RSS 约 77.3 MiB，`integrity_check` 约 127 ms。普通查询 p95 为 0.02–4.62 ms，200 条事务提交约 2.11 ms。 |
| macOS 文件语义     | 原始 Unicode 路径可保留；当前 APFS 同时接受 NFD 别名和大小写不同别名。符号链接目标移动后读取与 `realpath` 均为 `ENOENT`，硬链接共享设备与 inode。只读覆盖为 `EACCES`；打开文件仍可移动、删除并由原句柄继续读取；同路径 `rename` 会替换目标。                      |
| Windows 路径语法   | Node `path.win32` 已覆盖盘符、UNC、长路径命名空间和跨盘相对路径，但这不等同于 Windows 文件系统实测。验证入口为 `scripts/research-windows-file-semantics.mjs`；符号链接权限、文件占用、只读、大小写、长路径、原子提交和托管根跨卷迁移仍需在真实 runner 上完成。    |

文件语义验证模块的当前实测状态：

| 平台    | 状态     | 已知环境与结论                                                  |
| ------- | -------- | --------------------------------------------------------------- |
| macOS   | 已实测   | 2026-08-23；Darwin 24.6.0、arm64、APFS、Node.js 22.22.1，已通过 |
| Windows | 尚未实测 | 已准备下述脚本和指令，实际系统、架构与文件系统待运行时记录      |
| Linux   | 尚未实测 | 当前是兼容目标，尚未声明通过                                    |

首轮测试使用临时目录，已验证业务样本可以在 250 MiB 临时空间预算内分层执行；不需要在本机复制 10,000 或
20,000 份真实 PDF。

A1 可以照常进入实施计划和开发。当前文件语义验证模块已在 macOS 实测，Windows 结果保留为待测，不阻塞
数据库、领域模型、元数据、API 或 UI 等其他模块。以后修改路径、文件占用、移动、删除或原子提交逻辑时，同步
维护该兼容性脚本；若同一模块先在 Windows 验证，则反向补测 macOS。

Windows 本地 NTFS 使用 Node.js 22.22.1 或更高版本运行：

```powershell
node .\scripts\research-windows-file-semantics.mjs
```

需要验证移动盘、网络共享或云盘同步目录时，将测试根目录指向对应位置：

```powershell
node .\scripts\research-windows-file-semantics.mjs --root "D:\research-validation"
node .\scripts\research-windows-file-semantics.mjs --root "\\server\share\research-validation"
```

托管附件库迁移属于同一个文件语义测试模块。以下命令把源根保留在系统临时目录，并把复制目标放到另一盘或共享目录，
验证逐对象 hash、旧根保留和跨卷复制；随后运行实际 service、SQLite 切换与路由测试：

```powershell
node .\scripts\research-windows-file-semantics.mjs --managed-target "D:\research-validation"
node .\scripts\research-windows-file-semantics.mjs --managed-target "\\server\share\research-validation"
npx vitest run modules/research/src/files/managed-root-migration.test.ts modules/research/src/server/managed-root-routes.test.ts
```

脚本输出 JSON 结果；断言全部通过时退出码为 `0`，失败时为 `1`。所有样本创建在独立临时目录中，执行结束后
自动清理。

### 17.5 切片 A 实施验收状态

2026-08-24 已完成切片 A 的代码与自动验收。设计 §17.1 的 15 个场景均有自动测试，真实隔离 server 也完成了
managed 上传、识别、入库、回收恢复、托管根迁移和携带附件的迁移包导出；round-trip、SQLite 完整性和当前托管根
孤儿审计通过。10k/20k、200 文件和 256 MiB 专项均在独立临时目录运行并清理，详细数字与测试名称记录在实施计划
Task 15。

两套 Web 布局已通过类型检查和生产构建；当前运行环境没有可连接的浏览器，Safari 也未开启远程自动化，因此真实
点击、键盘和窄屏视觉验收仍保留为手动待测。Windows 文件语义与托管根跨卷迁移仍按 §17.4 的模块化指令待真机
运行，不影响其他已经验证的模块。

### 17.6 后续切片关键场景

1. **切片 B**：同一 PDF 在两个阅读上下文中拥有独立批注并可叠加显示；多个后台 PDF 休眠和关闭后回收渲染资源。
2. **切片 B**：大型 PDF 打开时不同时执行全文渲染、索引和 OCR；扫描型 PDF 提示后才进入可取消、可恢复的 OCR。
3. **切片 C**：证据卡片可以回到准确作品、版本、文件、页码和原文位置。
4. **切片 D**：按 §17.10 验证三格式导入导出、字段审查、未知字段、附件候选、citation key、CSL 引用、
   安全输出、扩展契约和 10,000 条规模。
5. **整体**：即使不使用 Zotero，全部核心功能仍然成立；离开 WorkBench 时仍有明确的数据导出路径。

### 17.7 切片 B0 技术验证清单

B0 只验证会影响切片 B 架构和预算的技术问题，不提前实现完整阅读器。验证脚本必须允许显式传入本地 PDF，
默认生成物和结果写入临时目录；私有语料的文件、文件名和绝对路径不进入提交。

测试语料至少覆盖：

| 类别       | 用途                                         | 来源与提交规则                         |
| ---------- | -------------------------------------------- | -------------------------------------- |
| 小型文本型 | 首屏、文本层、搜索和基础页面渲染             | 生成样本，可提交生成器                 |
| 中英文文本 | 字体、选择、复制、全文索引和中英文 OCR 对照  | 公开许可样本或生成样本                 |
| 扫描型     | 无文本层检测、用户确认、OCR、取消和恢复      | 生成图片 PDF 或公开许可样本            |
| 图片密集型 | 页面渲染峰值、缓存淘汰和快速跳页             | 公开许可样本或本地私有样本             |
| 超长文档   | 惰性加载、按页索引、安全模式和长时间资源变化 | 生成样本或本地私有样本                 |
| 损坏边缘   | 解析失败、超时、取消、错误展示和资源清理     | 由生成样本裁切或破坏，不含真实私有内容 |
| 加密文档   | 会话密码、错误密码和关闭后的凭据清理         | 生成样本                               |

B0 按以下模块执行并记录结果：

1. **PDF.js 基线**：记录页数、文件大小、首次文档可用、首次可见页面、顺序翻页、快速跳页、文本提取和失败退出。
2. **Range 路由**：验证 `206`、`Content-Range`、开放区间、后缀区间、非法区间 `416`、取消请求及路径授权；
   线性化大文件确认首屏使用部分读取。非线性化 PDF 可能因文件结构读取整份文件，需记录实际传输量并保持流式读取，
   不能把 Range 支持等同于所有 PDF 都能局部打开。
3. **资源回收**：分别测量单文档、多标签页、后台休眠和关闭后 Canvas、文本层、worker、任务及进程内存变化；
   关闭后等待固定观察窗口，不把垃圾回收时机当成严格的瞬时断言。
4. **全文索引**：验证每页 FTS5 建立、搜索定位、暂停、取消、断点续建、版本失效和完整重建；索引任务不得阻塞首屏。
5. **OCR**：先验证 Tesseract.js 的英语与简体中文识别、CPU、峰值内存、取消和恢复；记录测试文本的字符级或词级
   准确率。只有结果不足时再开展引擎对比。
6. **跨平台**：同一测试模块在哪个平台发生实现变更，就先在该平台运行并记录，另一平台保留待测；声明 B 的
   PDF 运行链路正式支持前，PDF.js、Range、OCR 和资源回收模块均需有 macOS arm64 与 Windows 11 x64 结果。

首轮目标设备矩阵如下，性能阈值在两端都有结果后定稿：

| 平台           | 设备与资源                                    | 文件系统 | Node.js          | 当前状态                                |
| -------------- | --------------------------------------------- | -------- | ---------------- | --------------------------------------- |
| macOS arm64    | MacBook Air `Mac15,12`，Apple M3 8 核，24 GiB | APFS     | 24.19.0 / 25.1.0 | Node、Range、FTS5、OCR、Edge 浏览器已测 |
| Windows 11 x64 | CPU 与内存随真机测试记录                      | NTFS     | 待记录           | 尚未进行 B0 实测                        |

每次结果至少记录日期、commit、平台、架构、文件系统、Node.js、浏览器版本、样本类别与大小、运行指令、冷启动或
热启动条件、时间和内存结果。Node 侧解析基准只说明解析与索引成本，不能代替真实浏览器的首屏和资源回收结果。
达到以下条件后 B0 完成：测试入口可重复运行，macOS 基线完成，Windows 对应指令已准备，架构风险已有结论；
正式性能预算可以先以 macOS 实测形成候选值，在 Windows 结果补齐后取两端较慢结果定稿，不阻塞不依赖平台差异的
B1 实现。

本轮已满足上述 B0 完成条件。下表数值直接作为 B1/B2 的 macOS 实施预算；Windows 11 x64 补测后，再按较慢
设备结果定稿双平台验收预算。

#### 2026-08-29 至 2026-08-30 macOS B0 结果

Node、Range、FTS5 与 OCR 基线基于 commit `1579506` 和 Node.js 24.19.0；真实浏览器生命周期复测基于
commit `5662500`、Node.js 25.1.0 与 Microsoft Edge 151.0.4129.107。运行环境均为 macOS 15.7.7、arm64、
APFS、Apple M3 8 核和 24 GiB 内存。`npm run research:b0` 会创建并清理独立临时目录，输出只使用生成样本 ID
或 `private-1` 等匿名编号。只有显式传入 `--keep-fixtures` 时保留生成语料；`--output` 指定的结果文件由调用者
使用后自行清理。

首轮语料包括 6 页小型文本样本、180 页/5.96 MB Range 样本、4 页无文本层样本、截断损坏样本、固定测试密码的
加密样本，以及四份只在本机使用的 PDF：52 页普通报告、1313 页超长文本、176 页/21.39 MB 无文本层扫描件和
5 页图片型文档。私有文件、文件名、路径和识别正文均未写入仓库。

| 验证模块           | macOS 实测结果                                                                                                                         | B1/B2 实施预算或结论                                                                                            |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| PDF.js Node 基线   | 全部可读样本成功；首个页面渲染最慢 232 ms；超长文档抽取 240 页为 644 ms                                                                | 样本集首个 Node 渲染页不超过 500 ms；抽取 240 页不超过 2 s                                                      |
| 损坏与扫描检测     | 截断文件返回 `InvalidPDFException`；176 页扫描件的文本页数为 0                                                                         | 失败不进入正式索引；无文本层只提示 OCR，不自动开始                                                              |
| 加密 PDF           | 缺少密码、错误密码和正确密码分别进入预期分支，正确密码后读取 6 页                                                                      | 密码只进入当前 loading task；应用层不得持久化或记录                                                             |
| Range              | 区间语义与 ID 授权通过；中断后活跃流归零；线性化 1313 页样本首屏读取 313–379 KiB                                                       | 生产路由保持流式读取；中断后 2 s 内归零；线性化样本三次远端跳页后不超过 1.5 MiB                                 |
| SQLite FTS5        | 663 页索引 18.74 ms，断点 331 页后重开续建成功，查询 0.29 ms，数据库 1.36 MB                                                           | 1000 页纯 FTS 写入不超过 250 ms，普通查询不超过 100 ms；正文抽取另计                                            |
| Tesseract.js 7.0   | 生成中英文准确率 100%；真实扫描页 OCR 880 ms；worker 峰值 236 MiB                                                                      | 首版保留该候选；冷启动不超过 3 s，普通单页不超过 3 s，worker 上限 384 MiB                                       |
| OCR 取消与恢复     | 独立子进程 3.82 ms 内终止，重新启动后结果一致，父进程内存回到启动前范围                                                                | OCR 必须进程隔离、默认单 worker、逐页落 checkpoint；取消预算不超过 250 ms                                       |
| 浏览器资源生命周期 | 三轮全新 Edge profile：首屏 119–247 ms，远端跳页 12–38 ms；4 文档、6 Canvas、6 文本层空闲后 heap 稳定，销毁后 DOM、task 与活动流均为 0 | 首屏不超过 750 ms；远端跳页不超过 500 ms；4 文档 heap 增量不超过 64 MiB；空闲或销毁观察窗内额外增长不超过 8 MiB |

Node 进程在销毁 PDF.js loading task 后，heap 会回到接近基线，但 RSS 和 external buffer 不保证立即归还操作系统；
因此 Node 结果只能证明任务已清理，不能代替 Canvas、文本层和 PDF.js worker 的浏览器回收验收。OCR 的
`worker.terminate()` 不提供可靠的在途 promise 收敛语义，首版采用独立子进程承载 OCR；取消时终止子进程，
重新启动后从最后完成页继续。

PDF.js 经本地 HTTP 路由打开线性化 5.62 MB/1313 页样本时，文档可用约 20–21 ms，首个页面渲染约 77–80 ms，
只产生 3 个 Range 请求并读取 313–379 KiB；loading task 销毁后活跃文件流为 0。相同设置打开 5.96 MB
非线性化生成样本时，会从文件尾向前读取全部区间并产生重复传输。这是 PDF 布局差异，不作为 route 失败；
B1 对非线性化文件显示正常加载进度并保持磁盘到响应的流式传输，不为了伪造“秒开”把整份文件读入内存。

真实浏览器三轮均使用全新 profile。1313 页样本从 `getDocument` 开始到首个 Canvas 与文本层完成为
119.3–247.2 ms，中页与末页跳转为 12.1–37.9 ms。主文档完成首、中、末三页后传输 0.55–0.86 MiB；加上
三个后台文档各一页后总传输 2.13–2.76 MiB，1.2 秒空闲观察窗内没有活动文件流。四个文档同时保留 6 个 Canvas、
6 个文本层和 410 个文本节点时，renderer JS heap 从 2.10 MiB 增至 3.69–3.72 MiB，空闲期保持稳定；销毁后
Canvas、文本层、文本节点、loading task 与活动文件流均为 0。销毁后的 heap 为 3.74–3.77 MiB，因此验收资源回收
以对象和任务归零、重复开关不持续增长为准，不要求 JavaScript 引擎立即把 heap 恢复到启动值。

`performance.memory` 记录的是 renderer JS heap，不代表整个浏览器的独占内存。B1 的真实浏览器 E2E 继续增加
同一进程内重复打开、休眠和关闭循环，并使用浏览器级内存指标检查长期增长；不把多个 Edge helper 的 RSS 简单相加。

当前验证依赖 `tesseract.js@7.0.0`，包许可证为 Apache-2.0。英语和简体中文语言包在 B3 发布前单独确认来源、版本和
随应用分发条件；运行时不从远端临时加载未固定版本的语言包。

带批注副本使用 `pdf-lib@1.17.1`，包许可证为 MIT。固定 fixture 已覆盖标准文本标记、区域、中文便笺、旋转页、
页书签扁平化和带增量更新段的输入；输出采用完整重写并由 PDF.js 重新打开核对页数。macOS arm64 的
`b3-annotated-pdf-export-and-recovery` 与四宽度 `b3-annotated-export-controls` 已实测；Windows 11 x64 保持
`not-run`，在该模块声明双平台兼容前运行 `node scripts/research-reader-compat.mjs --phase b3 --browser --ocr`。

macOS 复测命令：

```bash
npm run research:b0
npm run research:b0 -- --ocr --pdf "/path/to/text.pdf" --pdf "/path/to/scanned.pdf"
npm run research:b0 -- --browser --pdf "/path/to/large.pdf"
```

命令中的 `/path/to/...` 是占位符，运行前必须替换为本机真实 PDF 的绝对路径。

Windows 11 x64 在对应测试模块需要声明兼容时运行：

```powershell
npm run setup
npm run research:b0
npm run research:b0 -- --ocr --pdf "C:\path\to\text.pdf" --pdf "C:\path\to\scanned.pdf"
npm run research:b0 -- --browser --pdf "C:\path\to\large.pdf"
```

PowerShell 命令中的 `C:\path\to\...` 同样需要替换为 Windows 上真实存在的 PDF 路径。

浏览器命令默认寻找 Edge 或 Chrome；未找到时通过 `RESEARCH_B0_BROWSER` 指定可执行文件。Windows 结果需补充
CPU、内存、Node.js 和浏览器版本，并按相同匿名规则保存输出。该模块若先在 Windows 修改，则同理把 Windows
记为已测、macOS 记为待补测，不能沿用本次 macOS 结果。

### 17.8 切片 C 精确验收

| 编号 | 输入与操作                                                 | 必须观察到的结果                                                                                                                 |
| ---- | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| C-01 | 在阅读器中选择一段 PDF 文本并直接创建证据                  | 同一事务创建可见批注和证据；证据保存准确作品、版本、Asset、页码、quad、原文、上下文、Asset hash 和提取时间，回跳后定位到对应文本 |
| C-02 | 从区域批注、图表区域和 OCR 结果分别创建证据                | 非文本来源要求填写说明；OCR/PDF 来源类型保留；三类证据都能回到原页和区域，不伪造不存在的准确原文                                 |
| C-03 | 在通用层创建一张证据，并在两个命名上下文中引用             | 数据库仍只有一张证据和一份来源快照；两个上下文各自保存关系，界面显示原始来源上下文                                               |
| C-04 | 建立一个草稿观点，再用多篇作品中的证据分别支持、反驳和限定 | 无证据时明确显示草稿；三种关系可同时存在、独立编辑和解除，不复制或删除两端对象                                                   |
| C-05 | 建立含观点行和比较维度行的矩阵，加入多篇作品               | 列稳定关联 `Work`；系统按行列归集候选证据；用户可选择证据并填写单元格综合说明，每项都能回跳来源                                  |
| C-06 | 修订或删除来源批注、替换 PDF 版本并重建 OCR                | 相关证据和矩阵单元格分别显示来源变化与需要复核；原快照仍可查看，任何内容都不被静默重新绑定                                       |
| C-07 | 预览差异后把证据重新绑定到新版本中的位置                   | 产生新的证据 revision 和当前来源链接；旧来源快照、旧 revision 和变更原因仍可查询                                                 |
| C-08 | 创建跨论文研究笔记，并按正文、上下文、作品和对象类型检索   | FTS 与结构化筛选返回相同权限范围内的笔记、证据、观点和写作内容；命中项可回到知识对象或原文                                       |
| C-09 | 将笔记、证据、观点和矩阵引用加入写作板并导出               | 章节和块顺序保持；Markdown 含人类可读作品与页码及稳定来源标记；矩阵 Markdown/CSV 与当前行列和显式证据一致                        |
| C-10 | 并发编辑同一知识对象，再执行删除、恢复和解除关系           | 过期 revision 被拒绝并返回当前版本；删除写 tombstone；恢复不丢历史；解除关系不级联删除对象                                       |
| C-11 | 对含完整知识链的上下文执行保留归档和迁移归档               | 影响预览覆盖全部 C 对象；保留归档冻结整套内容；迁移时批注与知识归属一起移动，来源快照不改写                                      |
| C-12 | 回收、合并和尝试永久删除仍被有效证据引用的作品或附件       | 回收不删除知识；合并把当前关系重定向到幸存作品并保留快照；永久删除预览列出知识影响并阻止意外清理                                 |
| C-13 | 导出并重新导入包含 C 数据的规范 JSON 包                    | 笔记、证据快照、观点关系、矩阵、写作板、revision 和 tombstone 无损恢复；缺失附件明确报告但不破坏知识数据                         |
| C-14 | 在 1440、1024、768 和 390 像素宽度完成核心流程             | 文献库、研究知识页与阅读器之间的入口清楚；证据箱、矩阵和写作板在窄屏可操作，没有被遮挡的关键操作                                 |

### 17.9 切片 C 规模与测试边界

切片 C 的目标规模沿用切片 A 的 10,000 个 `Work`，增加 50,000 条批注、20,000 张证据卡片、5,000 个观点和
100 个矩阵；最大矩阵为 200 篇作品 × 50 行。数量规模使用临时磁盘 SQLite 生成，不复制等量 PDF，也不把私有论文
或可识别路径提交到仓库。

| 测试层      | 方法                                                                                         | 资源与平台边界                                            |
| ----------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 契约与领域  | 用固定 ID 和快照覆盖创建、复用、revision、tombstone、关系及来源状态                          | 进入默认测试套件，不需要浏览器或 PDF 文件                 |
| SQLite 规模 | 在独立临时目录生成目标数量，测量建库、分页、FTS、矩阵装载、revision 写入和 `integrity_check` | 测试结束自动清理；先记录 macOS 实测，再据实现结果确定阈值 |
| 来源闭环    | 使用 B 的生成 PDF、固定文本层、区域锚点和 OCR 代理验证创建、变化检测、重新绑定与回跳         | 不重复保存大型真实 PDF；真实私有语料只用于本机补充检查    |
| 浏览器流程  | 在四种宽度使用全新页面状态完成证据、观点、矩阵、写作和回跳                                   | 记录浏览器与平台；DOM 模拟不能代替真实点击与定位          |
| 文件输出    | 在临时目录验证 Markdown、CSV 和规范 JSON 的原子发布、覆盖确认、取消与清理                    | 涉及路径和文件替换时维护模块化 macOS/Windows 兼容记录     |

2026-08-30 的 macOS arm64 / APFS 正式表实测生成上述目标数量，建库 7.04 s、FTS 重建 5.53 s、统一检索
p95 25.12 ms、普通证据列表 37.39 ms、最大矩阵窗口 12.16 ms、单元格 revision 保存 0.92 ms，数据库
83.95 MiB、进程 RSS 303.03 MiB，`integrity_check` 与外键检查 355.45 ms。自动验收阈值保留足够的跨机器余量：
FTS 重建 20 s、检索 p95 1 s、普通列表/矩阵窗口/revision 保存各 500 ms、数据库 1 GiB、RSS 2 GiB。
该结果来自临时磁盘 SQLite，不使用 `:memory:`，测试结束会删除独立临时目录。C 的平台测试继续按 §3.7 以模块为
单位记录；文件输出和真实浏览器来源回跳在声明双平台兼容前补齐对应平台结果。

### 17.10 切片 D 精确验收

| 编号 | 输入与操作                                                               | 必须观察到的结果                                                                                                                             |
| ---- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01 | 分别导入含文章、图书章节、报告和不完整条目的 BibTeX、RIS、CSL JSON       | 每条记录形成独立原始记录和格式影子；公共语义映射到一个 `Work` 与可选 `Edition`；未知类型和缺失字段可以保存，不编造值                         |
| D-02 | 文件中间放入一条语法错误记录，前后保持有效记录                           | 有效记录继续预览；错误条目显示原文、序号、行号范围和解析错误；文件级边界损坏时停止提交，不静默跳过                                           |
| D-03 | 导入建议与现有标题、作者、日期和载体字段冲突                             | 每个字段独立选择；人工确认值默认保留；批量采用不能越过人工保护；过期 revision 提交被拒绝并返回最新差异                                       |
| D-04 | 用同一来源局部 key 再次导入相同内容和不同内容                            | 相同内容可识别为已处理；不同内容显示来源差异和候选动作，不自动更新或合并；最终报告区分采用、跳过、新版本和新作品                             |
| D-05 | 导入相同 DOI 不同文件、相似标题无 DOI 和相同 key 不同 DOI 的记录         | 复用现有重复候选规则；三种情况保持可区分，外部 key 不覆盖精确标识符与人工判断                                                                |
| D-06 | 记录包含 BibTeX `file`、RIS L1/L2、URL、相对路径和缺失路径               | 只生成分类型附件候选；默认不读、不复制、不移动；用户逐项选择后才进入 A 的托管/链接、hash、重复和回滚流程                                     |
| D-07 | 记录包含 keywords、note、annote 和未知长文本                             | keywords 只成为标签建议；note/annote 不创建 `ResearchNote`；原内容和未映射诊断可查看                                                         |
| D-08 | 导入含自定义 BibTeX 字段、未知 RIS tag 和 CSL 非标准键的记录后同格式导出 | 未修改记录可保留原始表达；修改一个已知字段后，新值生效且不冲突未知字段仍在；无法安全合并的字段逐项报告                                       |
| D-09 | 把三种源格式分别导出到另外两种格式                                       | 标题、作者、年份、载体和规范标识符等公共语义往返一致；源格式专有字段显示规范化、降级或未映射原因，不以“成功”掩盖损失                         |
| D-10 | 分别导出勾选项、当前目录、当前筛选和全部有效作品，并切换 preferred/all   | 预览数量与冻结 ID 清单一致；回收站默认排除；默认每个 Work 一条 preferred Edition，显式选项才输出全部 Edition；预览后 revision 变化会拒绝提交 |
| D-11 | 生成含重复作者年份标题、缺作者、缺年份和已有 BibTeX key 的 citation key  | 不冲突的原 key 保留；其余 key 可解释、可编辑且稳定；碰撞按稳定排序添加后缀；非法或重复手工 key 在导出前阻止                                  |
| D-12 | 对固定记录生成 APA、IEEE、Chicago author-date 引用和参考文献表           | 文内引用和参考文献顺序符合固定快照；纯文本、Markdown、净化 HTML 均可复制；样式或 locale 版本变化会显式更新快照                               |
| D-13 | 从文献库复制引用，并在 C 写作板插入引用与生成草稿参考文献表              | 文献库选择范围正确；写作块保留 `Work / Edition` 稳定 ID；刷新可以反映元数据变化，来源关系不退化成普通文本                                    |
| D-14 | 导出到新目标、已有目标、只读目标，并在写入和校验阶段取消                 | 已有目标先确认；同目录临时文件通过编码、记录数和摘要校验后原子发布；失败或取消保留旧目标并清理临时文件                                       |
| D-15 | 查询首版 `InteropAdapter` 能力并请求未实现能力                           | `records` 导入导出返回版本和能力；collections/tags/attachment-manifest/annotations/cursor 明确为 `unsupported`，没有空成功结果               |
| D-16 | 规范导出并恢复含来源记录、格式影子、字段选择和 key 偏好的数据库          | D 的真源数据与诊断完整恢复；缓存、导出预览和 CSL 结果可重建；旧 canonical v1/v2 继续可读，原文件不改写                                       |
| D-17 | 处理 10,000 条、至少 50 MiB 的三种输入并生成 10,000 条参考文献           | 记录数、诊断和输出校验正确；进度可见且可取消；单阶段不超过 120 秒、观测 RSS 不超过 2 GiB；临时输入与候选依赖执行后清理                       |
| D-18 | 在 1440、1024、768、390 像素宽度完成导入审查、导出预览和引用复制         | 长字段、错误、冲突和损失报告可读；关键确认不被遮挡；键盘和窄屏流程可完成；平台状态按对应测试模块分别记录                                     |

### 17.11 D0 技术验证记录

D0 使用 `scripts/research-interop-d0.mjs` 在操作系统临时目录安装固定候选版本，不修改仓库依赖或 lock 文件。正式模式
生成 10,000 条、每种格式至少 50 MiB 的匿名记录；快速模式按 1,000 条、5 MiB 检查脚本和环境。输入、候选依赖和
下载的固定 CSL style 在运行后清理。

候选信息以 [Citation.js](https://github.com/citation-js/citation-js)、
[Better BibTeX parser](https://github.com/retorquere/bibtex-parser)、
[citeproc-js](https://github.com/Juris-M/citeproc-js)、
[citeproc-rs](https://github.com/zotero/citeproc-rs) 和
[CSL 官方 styles](https://github.com/citation-style-language/styles) 为上游依据；版本与许可证同时读取本次固定 npm
包，不从搜索摘要推断。

比较结果如下：

| 候选或路径                                     | D0 观察                                                                                                                 | 决定                                                                                                 |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Citation.js 0.8.2 BibTeX/RIS/CSL 插件          | 9 条源格式 × 目标格式路径的公共语义往返一致；速度快；规范输出会丢 BibTeX 自定义字段、RIS 未知 tag 和 CSL 非标准顶层 key | 用作三格式规范映射与输出，不把其清理后的 CSL JSON 当作原始记录                                       |
| `@retorquere/bibtex-parser` 10.0.1             | 保留逐条输入、自定义字段、附件字段和局部解析错误；错误前后的有效条目仍可恢复；50 MiB 大文件明显较慢                     | 用于 BibTeX/BibLaTeX 格式影子、记录级诊断和未知字段；在可取消 worker 中运行并显示进度                |
| RIS 记录扫描器 + Citation.js                   | `TY/ER` 边界、原始 tag 与附件候选可独立保留，Citation.js 负责已知字段映射                                               | 不另引入低可信度 RIS 依赖；D1 实现窄的记录扫描与 tag 影子                                            |
| 原生 JSON + Zod + Citation.js                  | 原生对象可保留未知键，Citation.js 的标准 `custom` 可保留但任意非标准键会被清理                                          | CSL JSON 先逐项验证并保留原对象，再生成规范投影                                                      |
| Citation.js CSL 插件 / 直接 citeproc-js 2.4.63 | APA、固定版本 IEEE、Chicago author-date 的包装层与直接引擎输出一致；插件可以注册自定义 style                            | 选择 citeproc-js 并置于 WorkBench 适配器后；保留直接引擎一致性快照。生产依赖落锁前完成许可证义务核对 |
| citeproc-rs                                    | 官方仍标记为 work in progress，npm 只有多年未更新的 canary，不能作为当前稳定交付依赖                                    | 当前不采用；适配器边界允许以后用成熟版本替换                                                         |

2026-08-31 在 macOS arm64、Node.js 25.1.0 的正式结果：

| 模块                               | 10,000 条正式结果                                                                          |
| ---------------------------------- | ------------------------------------------------------------------------------------------ |
| 专用 BibTeX 解析                   | 52,431,058 bytes；22.727 s；最大观测 RSS 573.72 MiB；未知字段与局部错误保留                |
| Citation.js BibTeX 解析 / 序列化   | 52,431,058 bytes；0.372 s / 0.109 s；最大观测 RSS 301.00 MiB                               |
| Citation.js RIS 解析 / 序列化      | 52,431,060 bytes；0.182 s / 0.284 s；最大观测 RSS 456.09 MiB                               |
| Citation.js CSL JSON 解析 / 序列化 | 52,431,061 bytes；0.055 s / 0.089 s；最大观测 RSS 374.14 MiB                               |
| citeproc-js IEEE 参考文献表        | 10,000 条；引擎准备 0.246 s；生成 1.882 s；最大观测 RSS 394.38 MiB                         |
| APA / IEEE / Chicago 固定样式快照  | 文内引用快照通过；文本与 HTML 参考文献的 Citation.js 包装层和直接 citeproc-js 输出完全一致 |

正式与快速命令：

```bash
npm run research:d0
npm run research:d0 -- --quick
```

Windows 11 x64 的同模块保持 `not-run`。需要声明该模块兼容时，在 PowerShell 运行同一正式命令；脚本使用 `npm.cmd`、
系统临时目录和 Node 路径 API，不依赖 shell 路径语法：

```powershell
npm run research:d0
```

Windows 结果单独记录 Node.js、CPU、内存、耗时和 RSS，不从 macOS 结果推定通过。若 D 的解析、CSL 或文件输出模块
先在 Windows 修改，同理在 Windows 先验证对应模块，并把需要反向确认的 macOS 模块保持待测。

## 18. 明确暂缓或不做

- 不把视觉原型当作固定页面设计或阶段模型。
- 不以复刻 Zotero UI、数据库结构或全部插件为目标。
- 不在首版实现 Zotero 专用迁移、双向同步、Word 插件或团队协作库。
- 不在首版实现浏览器 Connector 或论文网页内直接抓取。
- 不在当前阶段引入桌面外壳；未来转 App 时重新进行完整设计。
- 不在首版实现标签自动聚类、自动批量整理或复杂标签组合。
- 不自动合并作品级候选重复，不自动定期覆盖元数据。
- 不自动清空回收站，不通过 WorkBench 删除链接位置的磁盘原文件。
- 不从第一天自研完整 CSL 引擎，优先采用成熟实现并保持输出适配层。
- 不默认把批注直接写回原 PDF。
- 不让 AI 未经确认自动创建大量标签、修改元数据或合并论文。
- 不让核心管理与阅读流程依赖内置 AI Runtime 或远程模型才能运行。
- 不在具体接入项目和需求尚未明确时锁定某一种 AI 供应商或 Agent 协议。
- 不在切片 C 实现完整论文项目、观点树、通用双向链接知识库、任意电子表格或完整文字处理器。
- 不在切片 C 自动生成证据、观点、矩阵综合说明或写作正文；AI 建议等待独立 Runtime 设计。
- 不在切片 C 实现正式引用样式、参考文献表、DOCX 或 LaTeX 输出；这些能力按切片 D 和后续写作需求推进。
- 不把实时多端协作、CRDT 或服务端常驻部署夹带进本模块。
- 不在设计未确认时创建详细 implementation plan 或数据库迁移。

## 19. 尚未决定的问题

产品大方向以及完整切片 A、B、C、D 的首版产品语义已经确认。以下是仍需完成的平台验证或后续能力问题：

1. **跨平台交付**：Windows、macOS 的安装和本地 server 启动方式，以及 Linux 兼容目标的明确
   支持边界。
2. **Windows 兼容性实测**：方便使用 Windows 环境时按 §17.4 的指令验证 NTFS 大小写与 Unicode 路径、
   符号链接权限、只读文件、打开文件的移动与删除、长路径、盘符与 UNC，以及同一托管根目录内的原子提交。
   当前状态为文件语义模块尚未在 Windows 实测；它不阻塞其他 A1 模块。在把文件语义模块标记为 Windows 兼容
   前完成；若该模块以后先在 Windows 发生实现变更，则反向补测 macOS。
3. **非固定磁盘场景**：在声明相应位置受支持前补测移动磁盘重连、网络盘中断和至少一种云盘占位文件；这些
   位置作为链接来源，不扩大为 A1 托管库。
4. **切片 B0 Windows 实测**：macOS 的首次可见页面、多文档空闲和关闭回收已经完成；Windows 11 x64 在
   对应模块声明兼容前运行同一脚本，最终双平台预算取两端较慢结果。
5. **切片 B0 OCR 发布语料**：Tesseract.js 已通过首轮中英文、真实扫描页、取消、恢复和进程隔离验证，当前不启动
   替代引擎对比；发布前继续用带人工真值的多版式中英文扫描页扩大准确率样本，并补 Windows 结果。
6. **内置 AI Runtime**：首版 Runtime 的能力、会话、任务、确认和审计边界；具体供应商 API 格式
   留待该项目设计时确定。

## 20. 各切片转入实现计划的条件

为某一切片创建 `docs/superpowers/plans/...` 前，先满足共同条件：

- 只收敛 §19 中影响当前实施模块的问题；平台相关模块按各自测试矩阵记录已测与待测状态。
- 明确该切片的用户流程、数据迁移、跨平台差异和失败恢复语义。
- 将 §17 中属于该切片的场景改写为精确验收标准。
- 确认新模块不要求 core 感知 research 领域。

除此之外，各切片或子里程碑分别满足：

- **完整切片 A**：产品语义以 §4–§8、§11.1、§12–§14 和 §16–§17 为准，不再把 A2 的核心数据需求留到 A1
  实施后决定。
- **子里程碑 A1**：技术选型、macOS/Node.js 22 文件验证、目标规模基准和精确验收已记录在 §8.4、§17.2–§17.4，
  可以进入实施计划和开发。实施中准备覆盖正常、文件名混乱、精确重复、同 DOI 不同文件、多版本、不完整元数据、
  扫描型、损坏、超大、只读、缺失和中断的可重复导入样本。文件语义模块的 Windows 实测按 §19.2 单独记录，
  不阻塞其他 A1 模块；该模块在哪个平台发生变更，就在该平台先验证并保留另一平台待测状态。
- **子里程碑 A2**：设计边界在 A1 开工前已确认；逐文件实施计划在 A1 闭环验收后编写，不改变已确认的稳定身份、
  字段来源、文件位置、删除恢复和迁移出口语义。
- **切片 B**：准备覆盖常见与极端情况的真实测试 PDF，完成 PDF 与 OCR 引擎最小技术验证，
  并据此设定性能预算。
- **切片 C**：§16 已确定批注 → 证据 → 观点 → 跨论文对照 → 写作 → 来源回跳的完整流程、上下文归属、
  来源快照、revision、删除恢复、上游生命周期、界面和 C1–C3 边界；§17.8–§17.9 已给出精确验收与规模测试，
  可以进入实施计划。
- **切片 D**：§8.3、§8.5、§13.2、§16 和 §17.10 已确定完整流程、字段映射、冲突、附件、引用输出、
  扩展契约和精确验收；§17.11 已完成 D0 选型与 10,000 条 / 50 MiB 基准，可以编写唯一实施计划。
  D1–D3 必须等该计划确认后才进入实现；完整 Zotero 迁移、附件备份和同步不作为开工条件。

内置 AI Runtime 另行建立产品切片和实施计划；仅预留能力注册与兼容边界不会阻塞上述切片。

切片 B 的 B1–B3 实现与 macOS 验收已经完成。阅读状态、虚拟页面、标签页休眠、通用与命名上下文批注、页级
FTS5、用户确认 OCR、任务恢复和带批注副本形成完整闭环；生成与固定语料覆盖 1000 页索引、180 页非线性化 PDF、
扫描代理、加密、损坏、缺失、旋转和增量更新段输入。真实私有大型 PDF 与扫描件尚未加入本轮语料。Windows 结果
继续按模块保留为待测，在正式声明对应模块双平台兼容和定稿双平台预算前补齐。

切片 C 已按 `docs/superpowers/plans/2026-08-30-research-workbench-slice-c.md` 完成 C1–C3 实施与
macOS arm64 最终验收。正式 Fastify + SQLite 流程贯通证据、三类观点关系、跨论文矩阵、四类写作引用、
Markdown/CSV 和来源回跳；统一检索、目标规模、canonical v1/v2、缺失附件恢复、四宽度浏览器操作和上游
生命周期回归通过。Windows 11 x64 的领域、文件输出、系统选择器、目标规模和真实浏览器模块保持 `not-run`。

切片 D 的 D0 已按 §17.11 完成 macOS arm64 技术验证，唯一实施计划位于
`docs/superpowers/plans/2026-08-31-research-workbench-slice-d.md`，当前等待计划确认。此状态不表示 D1–D3
已经开始；Windows 11 x64 的 D0 解析与 CSL 模块保持 `not-run`。

## 21. 变更记录

- 2026-08-31：确认切片 D 完整方向并完成 D0。确定导入审查、字段映射、来源局部 key、字段级冲突、附件候选、
  同格式未知字段保护、跨格式损失报告、四类导出范围、citation key、APA/IEEE/Chicago 引用输出和版本化
  `InteropAdapter` 边界；三格式 9 路语义往返与 10,000 条 / 50 MiB macOS 基准通过，形成唯一 D1–D3 计划并
  等待确认后实施。
- 2026-08-30：完成切片 C 的 C1–C3 实施和 macOS arm64 最终验收。正式服务流程贯通批注/证据、支持/反驳/限定、
  跨论文矩阵、四类写作引用、Markdown/CSV 和来源回跳；canonical v2 覆盖 A/B/C 真源与 revision，兼容 v1，
  支持空库恢复、缺失附件保留和失败回滚。14 个文件、67 项切片模块测试及 Edge 四宽度操作通过，Windows 对应
  模块继续准确标为待测。
- 2026-08-30：完成切片 C 统一检索与正式目标规模基准。笔记、证据、观点和写作正文共用 FTS5 索引，支持上下文、
  Work、对象类型、对象状态和来源状态筛选、稳定分页、重建与来源回跳；macOS arm64 的 10,000 Work、50,000 批注、
  20,000 证据、5,000 观点、100 矩阵及 200 × 50 最大矩阵在临时磁盘 SQLite 中通过资源和完整性阈值。
- 2026-08-30：确认切片 C 完整方向。确定继续以 `ReadingContext` 承载研究知识，加入轻量研究笔记、来源快照、
  可跨上下文复用的证据、支持/反驳/限定观点关系、观点或比较维度 × 作品矩阵、轻量写作板及 Markdown/CSV/JSON
  输出；统一 revision、tombstone、上下文归档迁移与上游删除保护语义，补齐 14 个精确验收场景和目标规模。
- 2026-08-30：完成切片 B 的 B1–B3 实现与 macOS 最终验收。端到端流程贯通阅读状态、两个命名上下文、批注、
  正文搜索、混合页 OCR 搜索和带批注副本；20 轮浏览器生命周期后 Canvas、文本层、loading task 和活动流归零，
  renderer heap 增长 0.23 MiB。生成 OCR 准确率为 1、取消 2.31 ms；四宽度 UI、1000 页索引、数据库完整性、
  派生缓存和原始 Asset hash 审计通过。Windows B1–B3 模块与真实私有 PDF 语料保持待测。
- 2026-08-30：完成 B0 macOS 真实 Edge 浏览器验证。三轮全新 profile 覆盖 1313 页线性化 PDF 的首屏、远端跳页、
  四文档并存、Canvas 与文本层、空闲观察、loading task 销毁和 Range 流回收；根据实测确定 B1/B2 macOS 实施预算，
  补充缓存失效与临时产物清理规则，切片 B 实施计划准入。
- 2026-08-29：完成 B0 的 macOS Node 首轮验证和跨平台脚本。生成与私有语料覆盖普通、超长、扫描、图片、损坏和
  加密 PDF；确认 Range 与 FTS5 可行，Tesseract.js 采用独立子进程、单 worker 和逐页 checkpoint。记录 macOS
  候选预算及 Windows 指令；进一步确认线性化 PDF 可局部打开、非线性化 PDF 可能读取全文，取消请求会关闭文件流。
- 2026-08-29：确认切片 B 作为一个完整切片实施，只编写一份计划并依次推进 B0–B3；确定直接使用 PDF.js、
  本地受控 Range 路由、Tesseract.js 首选验证、按页 FTS5、通用层与手工命名上下文、批注修订和 tombstone、
  新文件批注导出、会话密码、Research 界面边界及双平台性能基准，开始 B0 技术验证。
- 2026-08-24：完成切片 A 实施与自动验收。15 个功能场景、默认套件、200 文件、10k/20k、256 MiB、真实隔离
  server、迁移重跑、数据库完整性和托管对象孤儿审计通过；两套 Web 布局的真实浏览器点击/视觉验收与 Windows
  文件语义模块继续准确标为待测。
- 2026-08-23：明确跨平台验证以具体测试模块为单位，不按平台重复整个产品流程。当前文件语义模块已实测
  macOS APFS / arm64 / Node.js 22.22.1，Windows 与 Linux 尚未实测；保留 Windows 兼容性测试脚本和本地
  NTFS、移动盘、共享路径的运行指令，同一模块先在哪个平台变更就先验证该平台，再保留另一平台待测。
- 2026-08-23：完成 A1 首轮技术验证。确定 SHA-256 内容寻址、`pdfjs-dist` 基础识别、arXiv XML 解析、
  Crossref/DataCite/arXiv/OpenAlex 查询顺序、请求与缓存边界、路径身份规则和目标规模阈值；加入 A1 精确验收，
  记录 macOS/Node.js 22 实测，并保留 Windows 真机文件语义作为平台兼容性验证。
- 2026-08-23：确认在 A1 实施前先收敛完整切片 A，并将 A 固定拆分为 A1“论文导入与安全存储闭环”和
  A2“完整文献库治理与迁移出口”。补齐内容与位置分离、对象多重关系、可撤销合并、导入草稿与对账、
  元数据来源与离线行为、托管与链接生命周期、删除恢复、目录标签、结构化检索、可迁移导出和分层规模测试。
- 2026-08-22：确认首版包含切片 A、B、C，切片 D 只保留基础互操作与扩展接口；确认托管与链接
  文件并存、通用层 + 命名上下文层、完整标签治理基础、浏览器 + 本地 server、Windows/macOS
  正式支持与 Linux 兼容目标、阅读器首版能力、自动全文索引、可插拔 OCR 验证及研究知识闭环。
- 2026-08-22：拆分各产品切片的实施门槛，避免阅读器与批注决策反向阻塞可信文献库；补充
  WorkBench 内置 AI Runtime/Harness 的能力注册与兼容边界，参考常见接口形态但暂不绑定具体
  供应商 API 格式，并将 MCP 作为首选兼容目标。
- 2026-08-21：创建探索 spec；确认独立产品定位、视觉原型非约束、核心痛点、能力底线、
  两级去重、宽松元数据和 Zotero 次要互操作边界；保留存储模式、批注上下文、运行形态和
  首版范围为开放问题。
